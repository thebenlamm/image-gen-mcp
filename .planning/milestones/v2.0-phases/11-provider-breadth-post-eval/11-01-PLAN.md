---
phase: 11-provider-breadth-post-eval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/capabilities/photoroom-extract-subject.ts
  - src/capabilities/photoroom-composite-layers.ts
  - src/capabilities/fal-edit-prompt.ts
  - src/capabilities/ideogram-generate.ts
  - src/capabilities/types.ts
  - src/capabilities/validation.ts
  - src/capabilities/register.ts
  - src/task/plan-schema.ts
  - src/task/plan-validator.ts
  - src/eval/cases.ts
  - src/index.ts
  - tests/capabilities/photoroom-extract-subject.test.ts
  - tests/capabilities/photoroom-composite-layers.test.ts
  - tests/capabilities/fal-edit-prompt.test.ts
  - tests/capabilities/ideogram-generate.test.ts
  - tests/capabilities/registry-quality.test.ts
  - tests/task/plan-validator.test.ts
  - tests/task/templates.test.ts
  - README.md
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]
user_setup:
  - service: photoroom
    why: "Phase 11 second-provider for extract_subject AND composite_layers (D-04, PROV-01); used for product/subject preservation evals AND product compositing with shadow/relighting (the (with shadow) qualifier of PROV-01 is delivered through composite_layers per D-15)."
    env_vars:
      - name: PHOTOROOM_API_KEY
        source: "Photoroom Console -> https://app.photoroom.com/api-dashboard -> API Keys"
    dashboard_config: []
  - service: fal
    why: "Phase 11 second-provider mirror for edit_prompt via Flux Kontext (D-05, D-06, PROV-02, PROV-03)."
    env_vars:
      - name: FAL_KEY
        source: "fal.ai Dashboard -> https://fal.ai/dashboard/keys"
    dashboard_config: []
  - service: ideogram
    why: "Phase 11 introduces capability-layer 'generate' op (D-02, D-03, PROV-04) for text-fidelity evals."
    env_vars:
      - name: IDEOGRAM_API_KEY
        source: "Ideogram Developer Console -> https://developer.ideogram.ai/ -> API Keys"
    dashboard_config: []

must_haves:
  truths:
    - "Per D-01/D-04/PROV-01 (extract_subject half): Photoroom registers as a capability-only second provider for `extract_subject` via Remove Background API, gracefully skipped when PHOTOROOM_API_KEY is absent."
    - "Per D-01/D-04/D-15/PROV-01 (composite_layers half + (with shadow) qualifier): Photoroom registers as a capability-only second provider for `composite_layers` via the Image Editing API, which is where Photoroom's shadow/relighting product-photography output is delivered. The `(with shadow)` qualifier on PROV-01 is satisfied here, NOT on the extract_subject adapter (Remove Background API has no shadow output)."
    - "Per D-05/D-06/PROV-02/PROV-03: fal.ai registers as a capability-only second provider for `edit_prompt` (Flux Kontext model), gracefully skipped when FAL_KEY is absent."
    - "Per D-02/D-03/PROV-04: A new `generate` op is added to `CapabilityOp`, and Ideogram registers as the sole provider for `generate`, gracefully skipped when IDEOGRAM_API_KEY is absent. (Note: `composite_layers` is ALREADY in the `CapabilityOp` union — no enum change needed for the Photoroom composite_layers adapter; only `generate` requires the union expansion.)"
    - "Per D-12: Photoroom (both adapters) and fal register through `allowUnscoredProduction: true` with a non-empty `unscoredJustification` referencing pending Phase 11 eval cases; the registry's unscored-second-provider gate is NOT loosened."
    - "Per D-26: each new capability's `invoke` return surfaces `metadata.provider` and `metadata.modelVersion` so downstream Plan 02 trace enrichment has stable fields."
    - "Per D-28: `image_op` zod enum, `image_task` template validation, and PlanSchema.op enum all accept `generate` and the new providers without bypassing existing validation paths."
    - "Build-verification: after the `generate` enum entry lands in `src/capabilities/types.ts`, `npm run build` exits 0 BEFORE any of the four capability adapter files are written. This isolates TypeScript-exhaustiveness fallout from adapter implementation."
  artifacts:
    - path: "src/capabilities/photoroom-extract-subject.ts"
      provides: "createPhotoroomExtractSubjectCapability factory + Photoroom Remove Background API invocation (op: extract_subject)"
      exports: ["createPhotoroomExtractSubjectCapability"]
    - path: "src/capabilities/photoroom-composite-layers.ts"
      provides: "createPhotoroomCompositeLayersCapability factory + Photoroom Image Editing API invocation (op: composite_layers); delivers PROV-01 (with shadow) via shadow/relighting output of the Image Editing API"
      exports: ["createPhotoroomCompositeLayersCapability"]
    - path: "src/capabilities/fal-edit-prompt.ts"
      provides: "createFalEditPromptCapability factory + fal.ai Flux Kontext queue invocation"
      exports: ["createFalEditPromptCapability"]
    - path: "src/capabilities/ideogram-generate.ts"
      provides: "createIdeogramGenerateCapability factory + Ideogram 3.0 generate invocation"
      exports: ["createIdeogramGenerateCapability"]
    - path: "src/capabilities/types.ts"
      provides: "Updated `CapabilityOp` union containing 'generate' (composite_layers is already present — no change)"
      contains: "| 'generate'"
    - path: "src/task/plan-schema.ts"
      provides: "PlanNodeSchema op enum containing 'generate'"
      contains: "'generate'"
    - path: "src/task/plan-validator.ts"
      provides: "EXPECTED_OUTPUT_KIND map containing generate: 'image'"
      contains: "generate: 'image'"
  key_links:
    - from: "src/capabilities/register.ts"
      to: "createPhotoroomExtractSubjectCapability / createPhotoroomCompositeLayersCapability / createFalEditPromptCapability / createIdeogramGenerateCapability"
      via: "factory invocation + capabilityRegistry.register call"
      pattern: "createPhotoroomExtractSubjectCapability\\(\\)|createPhotoroomCompositeLayersCapability\\(\\)|createFalEditPromptCapability\\(\\)|createIdeogramGenerateCapability\\(\\)"
    - from: "src/capabilities/validation.ts"
      to: "generate op param check"
      via: "if (capability.op === 'generate') { ... }"
      pattern: "capability\\.op === 'generate'"
    - from: "src/index.ts"
      to: "image_op zod enum"
      via: "z.enum([..., 'generate'])"
      pattern: "z\\.enum\\(\\[.*'generate'.*\\]\\)"
    - from: "src/capabilities/photoroom-composite-layers.ts"
      to: "Photoroom Image Editing API"
      via: "POST to Photoroom Image Editing endpoint with shadow/relighting params"
      pattern: "image-api\\.photoroom\\.com|sandbox\\.photoroom\\.com"
---

<objective>
Land FIVE new capability adapters (Photoroom `extract_subject` via Remove Background API, Photoroom `composite_layers` via Image Editing API, fal.ai `edit_prompt` via Flux Kontext, Ideogram `generate`) plus the type/registration/validation surface they require, gated behind a Wave-0 build-verification step that proves the `generate` enum addition compiles before any adapter is written.

Purpose: Honor PROV-01 (BOTH halves: `extract_subject` AND `composite_layers (with shadow)`), PROV-02/03/04, D-01..D-06, D-12, D-15, D-28. The `(with shadow)` qualifier on PROV-01 is delivered through `composite_layers` because Photoroom's shadow/relighting output lives in the Image Editing API (D-15 explicitly says shadow/background quality is measured "where the Image Editing API is used"). The Remove Background API used by `extract_subject` produces a flat alpha cutout with no shadow.

This plan is the structural prerequisite for Plan 02's eval cases and trace enrichment. Capabilities register API-key-gated (graceful null on missing key) and use `allowUnscoredProduction: true` ONLY for second-providers (Photoroom×2, fal); Ideogram is the sole `generate` provider so the unscored gate does not fire (registry.ts line 24 guard).

Output: Five registry entries (when API keys present), an expanded `CapabilityOp` union (only `generate` is added — `composite_layers` is already in the union), an expanded `image_op` zod enum, and an expanded PlanSchema op enum. No eval cases or score application yet — that is Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md
@.planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from src/capabilities/types.ts and src/capabilities/registry.ts. -->
<!-- Use these directly. Do not re-derive by exploration. -->

From src/capabilities/types.ts (lines 1-9): `CapabilityOp` union ALREADY includes `'composite_layers'`. Only `'generate'` needs to be added. Verified in revision: do NOT re-add `composite_layers`.

From src/capabilities/types.ts (lines 91-123):
```typescript
export type CapabilityInvokeErrorCode =
  | 'INPUT_TOO_LARGE'
  | 'CONSTRAINT_VIOLATION'
  | 'PROVIDER_FAILURE'
  | 'TIMEOUT'
  | 'UNSUPPORTED';

export class CapabilityInvokeError extends Error {
  constructor(
    public readonly code: CapabilityInvokeErrorCode,
    message: string,
    public readonly retryable: boolean,
    public readonly suggestion?: string,
  ) { super(message); this.name = 'CapabilityInvokeError'; }
}

export interface Capability {
  op: CapabilityOp;
  provider: string;
  modelVersion: string;
  constraints: CapabilityConstraints;
  cost: CapabilityCost;
  latencyMsP50?: number;
  quality?: CapabilityQuality;
  invoke: CapabilityInvoke;
}
```

From src/capabilities/registry.ts (lines 11-48): `register(capability, options)` THROWS when `sameOpProviders.length > 0 && !hasScores && !options.allowUnscoredProduction`. When `allowUnscoredProduction: true` is passed, `quality.unscoredJustification` MUST be a non-empty string or registry throws (line 43-46). Both Photoroom adapters are second-providers (sharp owns composite_layers; @imgly/local owns extract_subject), so both require `allowUnscoredProduction: true` + non-empty `unscoredJustification`.

From src/capabilities/composite-layers.ts (line 88): existing `composite_layers` provider is `'sharp'` with `MODEL_VERSION = 'sharp-composite@1'`. Photoroom registers ALONGSIDE sharp, not as a replacement. Sharp continues to handle local deterministic composition; Photoroom is preferred only when eval scores demonstrate superior shadow/background quality (gated by Plan 02).

From src/capabilities/edit-prompt.ts (lines 41-47): use `resolveOptionalEnv(value)` to filter unresolved `${...}` placeholders BEFORE applying defaults — copy this helper verbatim into each new capability file.

From src/runs/trace.ts: `TraceNode.metadata` is sanitized in dag-executor.ts line 106-113 (Buffers/typed arrays stripped). Provider files MUST place `provider`, `modelVersion`, and `qualityMeasured` in the `metadata` field of the `kind: 'image'` return so Plan 02 can surface routing transparency without altering capability shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave-0 pre-flight): Add `generate` to CapabilityOp + propagate enum across plan-schema, plan-validator, eval/cases, image_op, validation; verify `npm run build` exits 0 before any adapter is written</name>
  <files>src/capabilities/types.ts, src/capabilities/validation.ts, src/task/plan-schema.ts, src/task/plan-validator.ts, src/eval/cases.ts, src/index.ts, tests/task/plan-validator.test.ts</files>
  <read_first>
    - src/capabilities/types.ts (lines 1-9 — current CapabilityOp union; verify `composite_layers` already present so do NOT add it)
    - src/capabilities/validation.ts (current op-block pattern lines 19-23 for edit_prompt — mirror for generate)
    - src/task/plan-schema.ts (PlanNodeSchema.op enum lines 17-26 — add `'generate'`)
    - src/task/plan-validator.ts (EXPECTED_OUTPUT_KIND map lines 45-54 — add `generate: 'image'`)
    - src/eval/cases.ts (CAPABILITY_OPS Set lines 17-26 — add `'generate'`)
    - src/index.ts lines 452-470 (image_op zod enum line 457 — add `'generate'`)
    - tests/task/plan-validator.test.ts (existing pattern for adding a new op to EXPECTED_OUTPUT_KIND test)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-02, D-03, D-28)
  </read_first>
  <behavior>
    - `CapabilityOp` union in `src/capabilities/types.ts` includes `'generate'` as a new entry (placed last to minimize diff). Do NOT touch `'composite_layers'` — it's already present.
    - `PlanNodeSchema.op` zod enum includes `'generate'`. `EXPECTED_OUTPUT_KIND` in plan-validator.ts maps `generate: 'image'`. `CAPABILITY_OPS` set in eval/cases.ts includes `'generate'`. `image_op` zod enum in index.ts line 457 includes `'generate'`. The surrounding tool description string at line 454 mentions `generate` in the image-returning ops list.
    - `validateCapabilityParams` adds a `generate` block requiring non-empty `params.prompt` (mirror the `edit_prompt` block at lines 19-23).
    - `npm run build` exits 0 with the new union member; this catches any switch/exhaustive checks elsewhere that need updating BEFORE adapter implementation begins.
    - Tests: `tests/task/plan-validator.test.ts` adds at minimum: a passing plan with one `generate` node (`outputKind: 'image'`, `terminalNodeId` matches) validates ok; a plan with `generate` op + `outputKind: 'data'` returns `errors[].code === 'PLAN_OUTPUT_KIND_MISMATCH'`. (You may need to extend a Capability fixture — search the existing test for the capability mock pattern and add `{ op: 'generate', provider: 'ideogram', modelVersion: 'ideogram-v3-0', constraints: {...}, cost: {...}, invoke: vi.fn() }`. The Ideogram capability file does not yet exist; that is intentional — this task wires the enum surface only.)
  </behavior>
  <action>
    1. Edit `src/capabilities/types.ts` line 1-9: add `| 'generate'` as the LAST entry of the `CapabilityOp` union. Do not reorder existing entries.
    2. Edit `src/capabilities/validation.ts`: add a new block immediately after the `edit_prompt` block (after line 23):
       ```typescript
       if (capability.op === 'generate') {
         if (typeof params.prompt !== 'string' || params.prompt.trim().length === 0) {
           throw new Error('generate requires params.prompt');
         }
       }
       ```
    3. Edit `src/task/plan-schema.ts` line 17-26: add `'generate',` to the `op` zod enum (last position, minimal diff).
    4. Edit `src/task/plan-validator.ts` lines 45-54: add `generate: 'image',` to `EXPECTED_OUTPUT_KIND`.
    5. Edit `src/eval/cases.ts` lines 17-26: add `'generate',` to `CAPABILITY_OPS` Set.
    6. Edit `src/index.ts` line 457: change the `image_op` `op` zod enum from `z.enum(['extract_subject', 'edit_prompt', 'composite_layers', 'transform', 'enhance_upscale', 'analyze_dimensions', 'analyze_palette', 'analyze_ocr'])` to include `'generate'` as the last entry. Update the surrounding tool description string at line 454 to mention `generate` in the image-returning ops list.
    7. Run `npm run build` and confirm exit 0. If TypeScript exhaustiveness checks elsewhere fire (switch statements over `CapabilityOp`), add a `case 'generate':` arm with the same handling as the most-similar existing op (`edit_prompt`) and re-run. Fix until build is clean.
    8. Extend `tests/task/plan-validator.test.ts` with the two `generate` cases described in `<behavior>`. Use a stub Capability literal with `op: 'generate'`, `provider: 'ideogram'`, `modelVersion: 'ideogram-v3-0'`. Do NOT import `createIdeogramGenerateCapability` (it does not exist yet — Task 3 creates it).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run tests/task/plan-validator.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s*\|\s*'generate'" src/capabilities/types.ts | grep -v '^#' | wc -l | tr -d ' '` returns at least `1`.
    - `grep -v '^#' src/task/plan-schema.ts | grep -c "'generate'"` returns at least `1`.
    - `grep -v '^#' src/task/plan-validator.ts | grep -c "generate: 'image'"` returns at least `1`.
    - `grep -v '^#' src/eval/cases.ts | grep -c "'generate'"` returns at least `1`.
    - `grep -v '^#' src/index.ts | grep -c "'generate'"` returns at least `1`.
    - `grep -v '^#' src/capabilities/validation.ts | grep -c "capability.op === 'generate'"` returns at least `1`.
    - `npm run build` exits 0.
    - `npx vitest run tests/task/plan-validator.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    `generate` is a first-class CapabilityOp throughout the system. Plan validation accepts `generate` nodes with `outputKind: 'image'` and rejects them with `outputKind: 'data'`. `image_op` accepts `op: 'generate'`. TypeScript compilation passes with no exhaustiveness errors. The plan now has a clean foundation on which Tasks 1–3 build adapter files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add Photoroom adapters — extract_subject (Remove Background API) AND composite_layers (Image Editing API, with shadow)</name>
  <files>src/capabilities/photoroom-extract-subject.ts, src/capabilities/photoroom-composite-layers.ts, tests/capabilities/photoroom-extract-subject.test.ts, tests/capabilities/photoroom-composite-layers.test.ts</files>
  <read_first>
    - src/capabilities/edit-prompt.ts (factory shape, env helper, AbortController timeout, response error mapping — analog for both Photoroom adapters)
    - src/capabilities/composite-layers.ts (existing sharp-based `composite_layers` provider — analog for params shape; Photoroom adapter must accept the SAME `params.layers` shape so plans are interchangeable at the routing layer per D-28)
    - src/capabilities/types.ts (Capability interface, CapabilityInvokeError codes, CapabilityInvokeResult union — note `composite_layers` already in CapabilityOp)
    - src/capabilities/registry.ts lines 24-48 (unscored-second-provider gate + unscoredJustification requirement; BOTH Photoroom adapters are second-providers)
    - src/utils/path-input-root.ts (assertWithinInputRoot — required for params.input validation)
    - tests/capabilities/edit-prompt.test.ts (test pattern: vi.stubGlobal('fetch', fetchMock), env-key beforeEach/afterEach)
    - tests/capabilities/composite-layers.test.ts (existing test for sharp composite — mirror params shape in Photoroom composite tests)
    - .planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md (Photoroom-specific notes lines 129-220)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-04, D-12, D-15, D-26 — note D-15 explicitly assigns shadow/background quality measurement to the Image Editing API path)
    - External: Photoroom API docs — Remove Background v2 endpoint AND Image Editing API endpoint, output shapes, billing distinction (per CONTEXT.md canonical_refs line 101)
  </read_first>
  <behavior>
    - **Photoroom extract_subject factory** (`createPhotoroomExtractSubjectCapability()`): returns `null` when `PHOTOROOM_API_KEY` is unset OR is a literal `${...}` placeholder. When key present: returns Capability with `op: 'extract_subject'`, `provider: 'photoroom'`, `modelVersion: 'photoroom-remove-bg-v2'`, `constraints: { requiresInputImage: true, supportsMultipleInputs: false, outputFormat: 'png' }`, `cost: { perCallUsd: 0.02 }`, `latencyMsP50: 3000`, `quality: { unscoredJustification: 'Phase 11 initial registration; eval scores pending from photoroom extract_subject eval cases (D-12, PROV-05)' }`. NOTE: this adapter does NOT produce shadow output — Remove Background API returns a flat alpha cutout. The `(with shadow)` qualifier of PROV-01 is delivered by the composite_layers adapter, not here.
    - **Photoroom extract_subject invoke**: POST to `https://image-api.photoroom.com/v2/segment` with `x-api-key: ${apiKey}` header (Photoroom standard auth — verify against current docs; if `Authorization: Bearer` is the documented form, use that and document the choice in the SUMMARY), multipart form-data `image_file=@<path>`. Use `AbortController` with 30s timeout. Map non-2xx → `CapabilityInvokeError('PROVIDER_FAILURE', ..., true)`; AbortError → `CapabilityInvokeError('TIMEOUT', 'Photoroom segment timed out after 30s', true)`. Return `{ kind: 'image', buffer, model: 'photoroom-remove-bg-v2', metadata: { input: filePath, provider: 'photoroom', modelVersion: 'photoroom-remove-bg-v2', api: 'remove-background', qualityMeasured: false } }`.
    - **Photoroom composite_layers factory** (`createPhotoroomCompositeLayersCapability()`): returns `null` when `PHOTOROOM_API_KEY` is unset/placeholder. When key present: returns Capability with `op: 'composite_layers'`, `provider: 'photoroom'`, `modelVersion: 'photoroom-image-editing-v1'` (constant — distinct from Remove Background version so registry's per-(op, provider, modelVersion) score keying is unambiguous), `constraints: { requiresInputImage: true, supportsMultipleInputs: true, outputFormat: 'png' }`, `cost: { perCallUsd: 0.05 }` (Image Editing API has distinct billing per CONTEXT.md canonical_refs line 101 — verify and adjust to actual Photoroom Image Editing pricing; if uncertain, use 0.05 as a conservative placeholder and document in SUMMARY for Phase 12 calibration), `latencyMsP50: 5000`, `quality: { unscoredJustification: 'Phase 11 initial registration; eval scores pending from photoroom composite_layers eval cases — measures shadow/background quality per D-15 (D-12, PROV-05, PROV-01 (with shadow))' }`.
    - **Photoroom composite_layers invoke**: validate params using the SAME shape as `src/capabilities/composite-layers.ts`: `params.canvas: {width, height}`, `params.layers: [{input, x?, y?, anchor?, scale?, opacity?}]`, plus a Photoroom-specific OPTIONAL `params.shadow: { enabled?: boolean, ... }` and `params.background: { color?: string, ... }` block to engage Image Editing API shadow/relighting. POST to the Photoroom Image Editing API endpoint (verify exact path against current docs; expected pattern is `https://image-api.photoroom.com/v2/edit` or similar — check at implementation time). Use multipart form-data with the foreground/background image fields and a JSON params block per Photoroom Image Editing docs. Use `AbortController` with 60s timeout (Image Editing is slower than Remove Background). Map errors as in extract_subject. Return `{ kind: 'image', buffer, model: 'photoroom-image-editing-v1', metadata: { provider: 'photoroom', modelVersion: 'photoroom-image-editing-v1', api: 'image-editing', shadowApplied: Boolean(params.shadow?.enabled), qualityMeasured: false } }`. When the Image Editing endpoint or its exact param schema is unclear from docs, the executor MUST capture the chosen endpoint, request schema, and any deviations in `11-01-SUMMARY.md` so Plan 02 evals reference the same surface.
    - Tests for BOTH adapters (vitest): each capability has at least: (a) returns null when API key absent (use `delete process.env.PHOTOROOM_API_KEY`), (b) constraint violation throws `CapabilityInvokeError` code='CONSTRAINT_VIOLATION' for missing input/canvas/layers, (c) successful path with `vi.stubGlobal('fetch', ...)` returning a valid response yields `kind: 'image'` with the mocked buffer + correct metadata fields (`provider: 'photoroom'`, `modelVersion`, `qualityMeasured: false`, plus `api: 'remove-background'` for extract OR `api: 'image-editing'` for composite), (d) HTTP non-OK maps to `PROVIDER_FAILURE` retryable=true, (e) AbortError maps to `TIMEOUT`. Composite test additionally exercises `params.shadow.enabled = true` → metadata reflects `shadowApplied: true`.
  </behavior>
  <action>
    1. Copy `resolveOptionalEnv` helper verbatim from `src/capabilities/edit-prompt.ts` lines 41-47 into both new files.
    2. Create `src/capabilities/photoroom-extract-subject.ts` exporting `createPhotoroomExtractSubjectCapability(): Capability | null`. Env var: `PHOTOROOM_API_KEY`. Endpoint: `https://image-api.photoroom.com/v2/segment`. Use `FormData` with the input file blob (read with `fs.promises.readFile`, wrap as `new Blob([buffer], { type: mime })`). Surface `metadata.qualityMeasured = false`.
    3. Create `src/capabilities/photoroom-composite-layers.ts` exporting `createPhotoroomCompositeLayersCapability(): Capability | null`. Env var: `PHOTOROOM_API_KEY` (same key, different endpoint). Mirror the params validation shape used by `src/capabilities/composite-layers.ts` (canvas/layers checks at lines 104-200) so a plan node targeting `composite_layers:photoroom` accepts the same input shape as `composite_layers:sharp` — Photoroom-specific extras (`shadow`, `background`) are OPTIONAL additive params. Use `assertWithinInputRoot` for every layer's `input` path (defense-in-depth, matching sharp adapter line 151).
    4. For both files, use `assertWithinInputRoot(filePath)` for `params.input` validation (see edit-prompt.ts line 87).
    5. Map all error paths to `CapabilityInvokeError` with the codes listed in `<behavior>`. The `unscoredJustification` strings MUST be non-empty (registry.ts line 43-46 throws otherwise).
    6. Tests live under `tests/capabilities/photoroom-extract-subject.test.ts` and `tests/capabilities/photoroom-composite-layers.test.ts`. Mirror beforeEach/afterEach env-management from `tests/capabilities/edit-prompt.test.ts` lines 9-22.
  </action>
  <verify>
    <automated>npx vitest run tests/capabilities/photoroom-extract-subject.test.ts tests/capabilities/photoroom-composite-layers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/capabilities/photoroom-extract-subject.ts | grep -c "export function createPhotoroomExtractSubjectCapability"` returns `1`.
    - `grep -v '^#' src/capabilities/photoroom-composite-layers.ts | grep -c "export function createPhotoroomCompositeLayersCapability"` returns `1`.
    - `grep -v '^#' src/capabilities/photoroom-extract-subject.ts | grep -c "PHOTOROOM_API_KEY"` returns at least `1`.
    - `grep -v '^#' src/capabilities/photoroom-composite-layers.ts | grep -c "PHOTOROOM_API_KEY"` returns at least `1`.
    - `grep -v '^#' src/capabilities/photoroom-composite-layers.ts | grep -c "op: 'composite_layers'"` returns at least `1`.
    - `grep -v '^#' src/capabilities/photoroom-composite-layers.ts | grep -c "photoroom-image-editing"` returns at least `2` (modelVersion declaration + return metadata).
    - `grep -v '^#' src/capabilities/photoroom-extract-subject.ts | grep -c "photoroom-remove-bg"` returns at least `2`.
    - `grep -c "unscoredJustification" src/capabilities/photoroom-extract-subject.ts src/capabilities/photoroom-composite-layers.ts` returns at least `2` total.
    - `grep -c "qualityMeasured: false" src/capabilities/photoroom-extract-subject.ts src/capabilities/photoroom-composite-layers.ts` returns at least `2` total.
    - `grep -E "CapabilityInvokeError\\('TIMEOUT'" src/capabilities/photoroom-extract-subject.ts src/capabilities/photoroom-composite-layers.ts | wc -l | tr -d ' '` returns at least `2`.
    - `npx vitest run tests/capabilities/photoroom-extract-subject.test.ts tests/capabilities/photoroom-composite-layers.test.ts` exits 0 with at least 5 tests per file.
  </acceptance_criteria>
  <done>
    Both Photoroom capability files compile under `npm run build`. Both test files pass. Factories return `null` when `PHOTOROOM_API_KEY` is unset. The composite_layers adapter accepts the same canvas/layers shape as the sharp incumbent so routing between them is interchangeable. The `(with shadow)` qualifier of PROV-01 is satisfied by the composite_layers adapter's Image Editing API call (per D-15). Registration happens in Task 4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fal.ai capability adapter (edit_prompt via Flux Kontext)</name>
  <files>src/capabilities/fal-edit-prompt.ts, tests/capabilities/fal-edit-prompt.test.ts</files>
  <read_first>
    - src/capabilities/edit-prompt.ts (factory shape, prompt validation, error mapping)
    - src/capabilities/enhance-upscale.ts (Replicate SDK queue/wait + `fetchWithTimeoutRetry` for URL output — analog for fal.ai queue model)
    - src/capabilities/types.ts (Capability interface, CapabilityInvokeError codes)
    - src/capabilities/registry.ts lines 24-48 (unscored-second-provider gate)
    - src/utils/path-input-root.ts (assertWithinInputRoot)
    - tests/capabilities/edit-prompt.test.ts and tests/capabilities/enhance-upscale.test.ts (test patterns)
    - .planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md (fal-specific notes lines 137-220)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-05, D-06, D-12, D-26)
  </read_first>
  <behavior>
    - fal factory: `createFalEditPromptCapability()` returns `null` when `FAL_KEY` is unset/placeholder. When present: `op: 'edit_prompt'`, `provider: 'fal'`, `modelVersion: 'fal-ai/flux-pro/kontext'` (constant; do NOT read from env — version drift bypasses registry.ts line 20-22 score invalidation), `constraints: { requiresInputImage: true, supportsMultipleInputs: false, maxPromptLength: 4000, supportedSizes: ['square', 'landscape', 'portrait'], outputFormat: 'png' }`, `cost: { perCallUsd: 0.04 }`, `latencyMsP50: 6000`, `quality: { unscoredJustification: 'Phase 11 initial registration; eval scores pending from fal-flux-kontext eval cases (D-12, PROV-05)' }`.
    - fal invoke: validate `params.input` (string, non-empty, `assertWithinInputRoot`), `params.prompt` (string, non-empty, len <= 4000). POST to `https://queue.fal.run/fal-ai/flux-pro/kontext` with `Authorization: Key ${apiKey}`, body `{ image_url: 'data:<mime>;base64,<b64>', prompt }`. fal returns either an immediate result or a queue object; for queue case poll `status_url` until `COMPLETED` (max 60s, 2s interval). Then GET the result, extract `images[0].url`, fetch via `fetchWithTimeoutRetry` (copy from enhance-upscale.ts) with 30s timeout, 1 retry. Return `{ kind: 'image', buffer, model: 'fal-ai/flux-pro/kontext', revisedPrompt: undefined, metadata: { input: filePath, provider: 'fal', modelVersion: 'fal-ai/flux-pro/kontext', requestId: <fal queue id>, qualityMeasured: false } }`. All error mapping uses `CapabilityInvokeError` with retryable=true for PROVIDER_FAILURE/TIMEOUT, false for CONSTRAINT_VIOLATION.
    - Tests (vitest): (a) returns null when `FAL_KEY` absent, (b) constraint violation throws `CapabilityInvokeError` code='CONSTRAINT_VIOLATION' for missing input/prompt, (c) successful path with `vi.stubGlobal('fetch', ...)` exercising queue submit -> poll status -> fetch result -> download image URL, asserts metadata fields (`provider: 'fal'`, `modelVersion: 'fal-ai/flux-pro/kontext'`, `qualityMeasured: false`, `requestId`), (d) HTTP non-OK on submit maps to `PROVIDER_FAILURE` retryable=true, (e) AbortError on image-URL download maps to `TIMEOUT`. fal queue test specifically: first fetch returns `{ status: 'IN_QUEUE', status_url, response_url }`, second fetch returns `{ status: 'COMPLETED' }`, GET response_url returns `{ images: [{ url }] }`, image URL fetch returns the buffer.
  </behavior>
  <action>
    1. Copy `resolveOptionalEnv` helper verbatim from `src/capabilities/edit-prompt.ts` lines 41-47.
    2. Create `src/capabilities/fal-edit-prompt.ts` exporting `createFalEditPromptCapability(): Capability | null`. Env var: `FAL_KEY`. Endpoint: `https://queue.fal.run/fal-ai/flux-pro/kontext`. Implement queue submit -> poll `status_url` (max 30 polls at 2s interval = 60s) -> fetch `response_url` -> download `images[0].url`. Use raw `fetch` (do NOT add `@fal-ai/client` to package.json — keep dependency surface minimal).
    3. Inline a copy of `fetchWithTimeoutRetry` from `enhance-upscale.ts` lines 26-55 (or extract to `src/capabilities/util-fetch.ts` if both this file and Task 3's Ideogram adapter would duplicate it — use your discretion, but if extracted both files import from it).
    4. Use `assertWithinInputRoot(filePath)` for `params.input` validation (see edit-prompt.ts line 87).
    5. Map all error paths to `CapabilityInvokeError` with the codes listed in `<behavior>`. `unscoredJustification` MUST be non-empty.
    6. Test lives under `tests/capabilities/fal-edit-prompt.test.ts`. Mirror env-management from `tests/capabilities/edit-prompt.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/capabilities/fal-edit-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/capabilities/fal-edit-prompt.ts | grep -c "export function createFalEditPromptCapability"` returns `1`.
    - `grep -v '^#' src/capabilities/fal-edit-prompt.ts | grep -c "FAL_KEY"` returns at least `1`.
    - `grep -v '^#' src/capabilities/fal-edit-prompt.ts | grep -c "fal-ai/flux-pro/kontext"` returns at least `1`.
    - `grep -c "unscoredJustification" src/capabilities/fal-edit-prompt.ts` returns at least `1`.
    - `grep -c "qualityMeasured: false" src/capabilities/fal-edit-prompt.ts` returns at least `1`.
    - `grep -E "CapabilityInvokeError\\('TIMEOUT'" src/capabilities/fal-edit-prompt.ts | wc -l | tr -d ' '` returns at least `1`.
    - `npx vitest run tests/capabilities/fal-edit-prompt.test.ts` exits 0 with at least 5 tests.
  </acceptance_criteria>
  <done>
    The fal Flux Kontext capability file compiles under `npm run build`. Tests pass. Factory returns `null` when `FAL_KEY` is unset. Registration happens in Task 4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Ideogram capability adapter (generate)</name>
  <files>src/capabilities/ideogram-generate.ts, tests/capabilities/ideogram-generate.test.ts</files>
  <read_first>
    - src/capabilities/edit-prompt.ts (factory + prompt validation pattern — analog for Ideogram)
    - src/capabilities/enhance-upscale.ts lines 26-55 (fetchWithTimeoutRetry — Ideogram returns ephemeral URLs that must be downloaded before TTL expires)
    - src/capabilities/types.ts (Capability interface; `'generate'` added to CapabilityOp by Task 0)
    - .planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md (Ideogram-specific notes lines 223-310)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-02, D-03, D-11, D-18)
  </read_first>
  <behavior>
    - `createIdeogramGenerateCapability()`: returns `null` when `IDEOGRAM_API_KEY` is unset/placeholder. When present: `op: 'generate'`, `provider: 'ideogram'`, `modelVersion: 'ideogram-v3-0'`, `constraints: { requiresInputImage: false, supportsMultipleInputs: false, maxPromptLength: 4000, supportedSizes: ['square', 'landscape', 'portrait'], outputFormat: 'png' }`, `cost: { perCallUsd: 0.08 }`, `latencyMsP50: 8000`. Note: Ideogram is the SOLE provider for `generate`, so the registry's unscored-second-provider gate does NOT fire — DO NOT pass `allowUnscoredProduction: true` (it would require unscoredJustification but is unnecessary; keep `quality` undefined until Plan 02 eval scores land per D-11).
    - Ideogram invoke: validate `params.prompt` (non-empty string, ≤4000 chars). Optional `params.size` maps to Ideogram aspect ratio (`square` → `ASPECT_1_1`, `landscape` → `ASPECT_16_9`, `portrait` → `ASPECT_9_16`). POST to `https://api.ideogram.ai/v1/ideogram-v3/generate` with header `Api-Key: ${apiKey}` and body `{ prompt, aspect_ratio, rendering_speed: 'DEFAULT', num_images: 1 }`. Response shape: `{ data: [{ url, seed }] }`. Download the URL with `fetchWithTimeoutRetry` (import shared util if extracted in Task 2, else inline copy) — Ideogram URLs are ephemeral (~1h TTL). Return `{ kind: 'image', buffer, model: 'ideogram-v3-0', metadata: { prompt, seed: data[0].seed, provider: 'ideogram', modelVersion: 'ideogram-v3-0', qualityMeasured: false } }`.
    - Tests: (a) returns null on missing key; (b) throws CONSTRAINT_VIOLATION on missing/empty prompt; (c) throws CONSTRAINT_VIOLATION on prompt > 4000 chars; (d) happy path mocks fetch to return `{ data: [{ url: 'https://...', seed: 42 }] }` then a buffer fetch and asserts metadata contains `provider: 'ideogram'`, `modelVersion: 'ideogram-v3-0'`, `qualityMeasured: false`, `seed: 42`; (e) HTTP non-OK on generate maps to `PROVIDER_FAILURE`; (f) AbortError on image download maps to `TIMEOUT`.
  </behavior>
  <action>
    1. Create `src/capabilities/ideogram-generate.ts` exporting `createIdeogramGenerateCapability(): Capability | null`. Env var: `IDEOGRAM_API_KEY`. Use the shared/inlined `fetchWithTimeoutRetry` for ephemeral URL download. Aspect ratio map MUST be defined as a const: `const ASPECT_MAP = { square: 'ASPECT_1_1', landscape: 'ASPECT_16_9', portrait: 'ASPECT_9_16' } as const;`. Constant model version: `const MODEL_VERSION = 'ideogram-v3-0';`. Surface `metadata.qualityMeasured = false`.
    2. Test lives under `tests/capabilities/ideogram-generate.test.ts` mirroring `tests/capabilities/edit-prompt.test.ts` env management.
  </action>
  <verify>
    <automated>npx vitest run tests/capabilities/ideogram-generate.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/capabilities/ideogram-generate.ts | grep -c "createIdeogramGenerateCapability"` returns at least `1`.
    - `grep -v '^#' src/capabilities/ideogram-generate.ts | grep -c "IDEOGRAM_API_KEY"` returns at least `1`.
    - `grep -v '^#' src/capabilities/ideogram-generate.ts | grep -c "ideogram-v3-0"` returns at least `2` (modelVersion declaration + return metadata).
    - `grep -c "qualityMeasured: false" src/capabilities/ideogram-generate.ts` returns at least `1`.
    - `npm run build` exits 0.
    - `npx vitest run tests/capabilities/ideogram-generate.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Ideogram capability factory returns `null` without `IDEOGRAM_API_KEY` and a fully-shaped `Capability` when present. Tests pass. Registration happens in Task 4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wire all four factories in register.ts + extend registry/templates tests + README docs</name>
  <files>src/capabilities/register.ts, tests/capabilities/registry-quality.test.ts, tests/task/templates.test.ts, README.md</files>
  <read_first>
    - src/capabilities/register.ts (current registration order + null-guard pattern lines 11-31)
    - src/capabilities/registry.ts lines 24-48 (unscored gate + unscoredJustification audit)
    - src/task/templates.ts lines 180-199 (matchTemplate registry validation — adding new providers must NOT bypass this)
    - tests/capabilities/registry-quality.test.ts (existing test for unscored second-provider gate; add coverage for Photoroom×2 + fal new-second-provider scenarios)
    - tests/task/templates.test.ts (existing template-match assertions; add a sanity test asserting `matchTemplate` still works after registering Photoroom×2/fal/Ideogram)
    - README.md (find the env vars section + capability/provider tables to extend)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-12, D-15, D-28)
  </read_first>
  <behavior>
    - `registerBuiltInCapabilities()` invokes the four new factories AFTER existing registrations so registration order is: extract_subject (@imgly/local), edit_prompt (openai), transform, analyze_dimensions, analyze_palette, composite_layers (sharp), enhance_upscale, analyze_ocr, **photoroom-extract-subject, photoroom-composite-layers, fal-edit-prompt, ideogram-generate**.
    - Photoroom extract_subject registers via `capabilityRegistry.register(photoroomExtract, { allowUnscoredProduction: true })` because `@imgly/local` is already a same-op provider.
    - Photoroom composite_layers registers via `capabilityRegistry.register(photoroomComposite, { allowUnscoredProduction: true })` because `sharp` is already a same-op provider for `composite_layers`.
    - fal registers via `capabilityRegistry.register(falEditPrompt, { allowUnscoredProduction: true })` because `openai` is already a same-op provider.
    - Ideogram registers via plain `capabilityRegistry.register(ideogramGenerate)` (no options) because `generate` has no incumbent.
    - When API keys are absent, the corresponding factory returns `null` and `register.ts` MUST guard with `if (factory) { ... }` so absence is non-fatal.
    - `tests/capabilities/registry-quality.test.ts` adds: (a) registering a Photoroom-shaped capability without `allowUnscoredProduction: true` THROWS the unscored-gate error from registry.ts line 36-38; (b) registering with `allowUnscoredProduction: true` BUT empty `unscoredJustification` THROWS line 44-46 error; (c) registering with both `allowUnscoredProduction: true` AND non-empty justification SUCCEEDS. Cover BOTH `extract_subject` and `composite_layers` second-provider scenarios so the new composite_layers second-provider path is exercised.
    - `tests/task/templates.test.ts` adds: with all three new env vars set in `process.env` and capabilities registered, `matchTemplate({ goal: 'product-on-white', inputImages: { input_0: '/abs/path/img.png' }})` still returns a `TemplateMatch` whose plan nodes all resolve via `registry.get` (D-28).
    - README documents `PHOTOROOM_API_KEY`, `FAL_KEY`, `IDEOGRAM_API_KEY` in the env-var section and lists the new capabilities (`extract_subject` via `photoroom`, `composite_layers` via `photoroom` (with shadow), `edit_prompt` via `fal`, `generate` via `ideogram`) in the capability/provider table.
  </behavior>
  <action>
    1. Edit `src/capabilities/register.ts`:
       - Add four imports at top: `import { createPhotoroomExtractSubjectCapability } from './photoroom-extract-subject.js';`, `import { createPhotoroomCompositeLayersCapability } from './photoroom-composite-layers.js';`, `import { createFalEditPromptCapability } from './fal-edit-prompt.js';`, `import { createIdeogramGenerateCapability } from './ideogram-generate.js';`.
       - After the existing `createAnalyzeOcrCapability()` registration (after line 28), add four null-guarded registration blocks in this exact order:
         ```typescript
         const photoroomExtract = createPhotoroomExtractSubjectCapability();
         if (photoroomExtract) {
           capabilityRegistry.register(photoroomExtract, { allowUnscoredProduction: true });
         }
         const photoroomComposite = createPhotoroomCompositeLayersCapability();
         if (photoroomComposite) {
           capabilityRegistry.register(photoroomComposite, { allowUnscoredProduction: true });
         }
         const falEditPrompt = createFalEditPromptCapability();
         if (falEditPrompt) {
           capabilityRegistry.register(falEditPrompt, { allowUnscoredProduction: true });
         }
         const ideogramGenerate = createIdeogramGenerateCapability();
         if (ideogramGenerate) {
           capabilityRegistry.register(ideogramGenerate);
         }
         ```
    2. Edit `tests/capabilities/registry-quality.test.ts`:
       - Add a `describe('Phase 11 second-provider gate')` block covering the three sub-cases for BOTH Photoroom adapters AND fal. Use minimal inline Capability literals — do NOT import the real factories. Use a fresh `new CapabilityRegistry()` per test. Pre-register a same-op incumbent first (e.g. `{ op: 'extract_subject', provider: 'fake-incumbent', modelVersion: 'v1', ... }` for the extract case; `{ op: 'composite_layers', provider: 'fake-sharp', modelVersion: 'v1', ... }` for the composite case), then assert behaviors for the second registration.
    3. Edit `tests/task/templates.test.ts`:
       - Add an assertion using a stub registry that includes Photoroom×2, fal, and Ideogram entries alongside the incumbents, and verify `matchTemplate({ goal: 'product-on-white', ... })` returns a non-null `TemplateMatch`. Reuse the file's existing stub-registry pattern.
    4. Edit `README.md`:
       - In the env vars section, append three rows: `PHOTOROOM_API_KEY` ("Photoroom API key. Enables `extract_subject` (Remove Background API) AND `composite_layers` (Image Editing API, with shadow/relighting) via `photoroom`."), `FAL_KEY` ("fal.ai API key. Enables `edit_prompt` via `fal` (Flux Kontext) as a faster/cheaper mirror to `openai`."), `IDEOGRAM_API_KEY` ("Ideogram Developer API key. Enables `generate` via `ideogram` for text-fidelity generation.").
       - In the capability table (or `image_op` capabilities section), add four rows mapping op → provider → API key requirement → notes (one row per Photoroom adapter so users see both surfaces).
       - Note explicitly: "Photoroom (both surfaces), fal, and Ideogram are exposed via `image_op` once their API keys are present. `image_task` will only prefer them once Phase 11 eval cases populate quality scores (see Plan 02). The Photoroom `composite_layers` adapter delivers the `(with shadow)` qualifier of PROV-01 via the Image Editing API; the `extract_subject` adapter uses the simpler Remove Background API and produces a flat alpha cutout."
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx vitest run tests/capabilities/registry-quality.test.ts tests/task/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createPhotoroomExtractSubjectCapability" src/capabilities/register.ts` returns at least `2` (import + invocation).
    - `grep -c "createPhotoroomCompositeLayersCapability" src/capabilities/register.ts` returns at least `2`.
    - `grep -c "createFalEditPromptCapability" src/capabilities/register.ts` returns at least `2`.
    - `grep -c "createIdeogramGenerateCapability" src/capabilities/register.ts` returns at least `2`.
    - `grep -c "allowUnscoredProduction: true" src/capabilities/register.ts` returns at least `4` (existing enhance_upscale + new Photoroom×2 + new fal). Ideogram MUST NOT have this option in its `register(...)` call.
    - `grep -E "ideogramGenerate\\)" src/capabilities/register.ts | grep -v "allowUnscoredProduction" | wc -l | tr -d ' '` returns at least `1` (Ideogram registration without options).
    - `grep -c "PHOTOROOM_API_KEY\|FAL_KEY\|IDEOGRAM_API_KEY" README.md` returns at least `3`.
    - `grep -c "with shadow\|Image Editing API" README.md` returns at least `1` (PROV-01 (with shadow) qualifier explicitly documented).
    - `npx vitest run tests/capabilities/registry-quality.test.ts tests/task/templates.test.ts` exits 0.
    - `npm run build` exits 0.
    - With all three API keys set and `dist/index.js` started, manual smoke (NOT in automated gate but documented in done): calling `list_capabilities` returns entries for `extract_subject:photoroom`, `composite_layers:photoroom`, `edit_prompt:fal`, `generate:ideogram`.
  </acceptance_criteria>
  <done>
    All four new capabilities are wired into `registerBuiltInCapabilities()`. Registration is API-key-gated. The unscored-second-provider gate is preserved (D-12) and exercised by tests for BOTH Photoroom adapters. Templates fast-path still validates against the expanded registry (D-28). README documents the new env vars, both Photoroom surfaces, and the (with shadow) qualifier resolution. `npm run build` and the targeted vitest suites pass.
  </done>
</task>

</tasks>

<verification>
After all five tasks (Task 0 + Tasks 1–4):

```bash
# Full build + targeted tests
npm run build
npx vitest run tests/capabilities/ tests/task/plan-validator.test.ts tests/task/templates.test.ts tests/task/plan-schema.test.ts tests/eval/cases.test.ts

# Source surface checks
grep -v '^#' src/capabilities/types.ts | grep -c "'generate'"
grep -v '^#' src/task/plan-schema.ts src/eval/cases.ts src/index.ts | grep -c "'generate'"

# Registration order + gate hygiene
grep -A2 "createPhotoroomExtractSubjectCapability\|createPhotoroomCompositeLayersCapability\|createFalEditPromptCapability\|createIdeogramGenerateCapability" src/capabilities/register.ts

# PROV-01 BOTH halves coverage
grep -c "op: 'extract_subject'" src/capabilities/photoroom-extract-subject.ts  # >= 1
grep -c "op: 'composite_layers'" src/capabilities/photoroom-composite-layers.ts  # >= 1
```

Each of the four PROV requirements PROV-01 (BOTH halves), PROV-02, PROV-03, PROV-04 has at least one corresponding capability file registered in `register.ts`. PROV-01 is satisfied by TWO capability files (extract_subject + composite_layers), with the `(with shadow)` qualifier delivered by the composite_layers adapter per D-15. PROV-05 is satisfied jointly with Plan 02's eval cases. `npm run build` must exit 0.
</verification>

<success_criteria>
1. `src/capabilities/photoroom-extract-subject.ts`, `src/capabilities/photoroom-composite-layers.ts`, `src/capabilities/fal-edit-prompt.ts`, `src/capabilities/ideogram-generate.ts` exist and export their factory functions.
2. `CapabilityOp` union, `PlanNodeSchema.op` enum, `image_op` zod enum, and `CAPABILITY_OPS` Set all include `'generate'`. `composite_layers` was already in the union (no change required there).
3. `EXPECTED_OUTPUT_KIND` in plan-validator.ts maps `generate: 'image'`.
4. `validateCapabilityParams` enforces non-empty `params.prompt` for `generate`.
5. `register.ts` wires all four new factories; both Photoroom adapters and fal use `allowUnscoredProduction: true` with non-empty justifications; Ideogram does not (sole-provider — D-11 trace surfacing handled in Plan 02).
6. PROV-01 is FULLY satisfied: `extract_subject` (Photoroom Remove Background API) AND `composite_layers (with shadow)` (Photoroom Image Editing API). The `(with shadow)` qualifier is delivered by the composite_layers adapter, documented in README, and traceable through `metadata.api: 'image-editing'`.
7. All new and modified test files pass.
8. `npm run build` exits 0 (verified at end of Task 0 BEFORE adapters are written, AND again after Task 4).
9. README documents the three new env vars, all four capability rows, and the (with shadow) qualifier resolution.
</success_criteria>

<output>
After completion, create `.planning/phases/11-provider-breadth-post-eval/11-01-SUMMARY.md` recording:
- Provider adapters created and which API endpoints they hit (note both Photoroom endpoints distinctly)
- Any deviations from PATTERNS.md (e.g., Photoroom auth header form, fal queue polling cadence, exact Photoroom Image Editing API endpoint and request schema discovered at implementation time)
- Whether `fetchWithTimeoutRetry` was inlined or extracted to a shared util
- Confirmed list of registered capabilities when the relevant env vars are present (should include BOTH `extract_subject:photoroom` AND `composite_layers:photoroom`)
- Notes for Plan 02 on how `metadata.qualityMeasured` should flip to `true` post-eval, and which scorers map to the `composite_layers:photoroom` shadow/background quality measurement per D-15
- Cost calibration note: actual Photoroom Image Editing API per-call price observed during testing (vs. the 0.05 placeholder used in the adapter), so Phase 12 can adjust if needed
</output>
