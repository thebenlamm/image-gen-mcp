---
phase: 11-provider-breadth-post-eval
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - eval/cases/photoroom.json
  - eval/cases/fal-flux-kontext.json
  - eval/cases/ideogram.json
  - src/eval/run.ts
  - src/eval/apply-results.ts
  - src/task/dag-executor.ts
  - src/task/planner.ts
  - src/task/serialize-response.ts
  - tests/eval/cases.test.ts
  - tests/eval/run.test.ts
  - tests/eval/apply-results.test.ts
  - tests/task/dag-executor.test.ts
  - tests/task/planner.test.ts
  - README.md
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04, PROV-05]

must_haves:
  truths:
    - "Per PROV-05/D-13/D-15 (Photoroom extract_subject half): at least one Photoroom eval case for `op: 'extract_subject'` exists with `requiredEnv: ['PHOTOROOM_API_KEY']` and uses deterministic scorers (`alpha_coverage` + `pixel_delta`) measuring product/subject preservation."
    - "Per PROV-05/D-15/PROV-01 ((with shadow) qualifier): at least one Photoroom eval case for `op: 'composite_layers'` exists with `requiredEnv: ['PHOTOROOM_API_KEY']`, exercises the Image Editing API path (where shadow/relighting output is produced), and uses `pixel_delta` plus an alpha-edge-quality measure (`alpha_coverage` on the composited result OR a documented baseline-comparison via the pre-supplied composite-bg/composite-overlay fixtures). This case satisfies the second half of PROV-01 and the (with shadow) qualifier."
    - "Per PROV-05/D-16: at least one fal Flux Kontext eval case exists with `requiredEnv: ['FAL_KEY']` running on the SAME fixture and prompt as an existing OpenAI `edit_prompt` case so head-to-head comparison is valid."
    - "Per PROV-05/D-17: at least one fal Flux Kontext eval case scores instruction success via OCR (`ocr_text_presence`) and includes `expectedText`."
    - "Per PROV-05/D-18: at least one Ideogram eval case exists with `requiredEnv: ['IDEOGRAM_API_KEY']` and uses `ocr_text_presence` with `expectedText` to measure text fidelity."
    - "Per D-26/D-09: `dag-executor.ts` enriches every TraceNode's `metadata` with `qualityMeasured: boolean` and (when scored) the relevant per-scorer score(s); when no quality is measured, metadata says `qualityMeasured: false`."
    - "Per D-11: when a capability is the SOLE provider for its op (Ideogram for `generate` until/unless a second provider lands), trace surfaces `noIncumbentComparison: true` so users see the routing was uncontested."
    - "Per D-25: when the planner routes purely on cost/latency because no quality scores exist for any legal provider, the response trace contains `qualityUnavailable: true` for those nodes (visible in the serialized response)."
    - "Per D-27: there is no silent fallback path. The provider-failure tests assert that on `CapabilityInvokeError`, the structured error appears in the trace with `errorDetail.code` and the executor does NOT swap providers."
    - "Per D-04/D-05/D-12: after eval cases run with API keys set, `applyEvalResultsToRegistry` populates `quality.scores` for the new providers; `registry.listScored('extract_subject')` includes `photoroom`; `registry.listScored('composite_layers')` includes `photoroom`; `registry.listScored('edit_prompt')` includes `fal`; `registry.listScored('generate')` includes `ideogram`."
    - "Per D-07/D-08: `image_op` allows direct exploration of any registered provider once API keys are present and capability validation passes (no eval gate); `image_task` planner preference, however, requires eval scores to exist before a new provider is preferred over an incumbent. Both behaviors are exercised by Plan 02 tests."
    - "Per D-10: the displacement-edge thresholds (≥0.03 absolute quality-score improvement OR ≥20% latency/cost improvement when quality is above the floor) are documented in the README routing section so a downstream planner-prompt change can encode them deterministically; until then, the trace surfaces the per-scorer scores so the LLM planner can apply this rule itself."
    - "Per D-14/D-19: every Phase 11 eval case uses deterministic/programmatic scorers only (`alpha_coverage`, `pixel_delta`, `ocr_text_presence`); no broad subjective/human-only quality gate is introduced. Subjective inspection is supported by persisting comparable output artifacts, not by adding a human-preference workflow."
    - "Per D-20/D-21/D-22/D-23/D-24: the planner system-prompt update aligns capability routing with `quality_tier` semantics — `best` picks highest relevant measured quality within budget/latency caps, `fast` requires a quality floor then chooses lowest latency with cost as tiebreaker, `balanced` weighs relevant quality first then cost then latency, and goal-specific metrics (text fidelity for text-heavy goals, subject/shadow/background preservation for product goals, instruction success + preservation for edits) win over generic provider scores. Trace surfaces WHICH scorer drove the routing via `metadata.qualityScores`."
  artifacts:
    - path: "eval/cases/photoroom.json"
      provides: "Photoroom eval cases for BOTH extract_subject (Remove Background API) AND composite_layers (Image Editing API, with shadow per D-15) — single provider file covering both Photoroom adapters from Plan 01"
      contains: "\"provider\": \"photoroom\""
    - path: "eval/cases/fal-flux-kontext.json"
      provides: "fal.ai Flux Kontext eval cases mirroring OpenAI fixtures for head-to-head"
      contains: "\"provider\": \"fal\""
    - path: "eval/cases/ideogram.json"
      provides: "Ideogram text-fidelity eval cases with OCR-backed scoring"
      contains: "\"provider\": \"ideogram\""
    - path: "src/task/dag-executor.ts"
      provides: "TraceNode metadata enriched with qualityMeasured / qualityScores / noIncumbentComparison fields"
      contains: "qualityMeasured"
  key_links:
    - from: "eval/cases/*.json"
      to: "src/eval/run.ts -> applyEvalResultsToRegistry"
      via: "npm run eval reads cases, runs invocations, writes result JSON, then calls applyEvalResultsToRegistry"
      pattern: "applyEvalResultsToRegistry\\(capabilityRegistry"
    - from: "eval/cases/photoroom.json composite_layers cases"
      to: "src/capabilities/photoroom-composite-layers.ts (Plan 01) -> registry.listScored('composite_layers')"
      via: "Image Editing API invocation produces shadow/background output; pixel_delta scorer measures composited result vs reference; applyEvalResultsToRegistry populates quality.scores keyed on (composite_layers, photoroom, photoroom-image-editing-v1)"
      pattern: "composite_layers.*photoroom|photoroom.*composite_layers"
    - from: "src/task/dag-executor.ts runNodeWithRetry"
      to: "TraceNode.metadata.qualityMeasured / qualityScores / noIncumbentComparison"
      via: "registry.get + registry.list to compute incumbent count + scored status, merged into sanitizedMetadata"
      pattern: "qualityMeasured|noIncumbentComparison"
    - from: "src/task/serialize-response.ts"
      to: "Trace nodes preserve qualityMeasured / qualityUnavailable / noIncumbentComparison through serialization"
      via: "Trace passes through serialization unchanged; new metadata fields are JSON-safe primitives"
      pattern: "qualityMeasured|qualityUnavailable|noIncumbentComparison"
---

<objective>
Add the eval cases that PROV-05 requires (covering BOTH Photoroom adapters from Plan 01 — extract_subject AND composite_layers, fal Flux Kontext, and Ideogram), ensure their results populate `quality.scores` on the new capabilities (existing pipeline already does this — verify it works for `generate` op AND for `composite_layers:photoroom`), and make the planner/executor's routing-transparency observable in the trace per D-09/D-11/D-25/D-26/D-27.

Purpose: This plan converts Plan 01's "registered but unmeasured" capabilities into "measured" ones via deterministic eval cases (D-13: small + hard-nosed). The Photoroom `composite_layers` case specifically measures the (with shadow) qualifier of PROV-01 by exercising Photoroom's Image Editing API path per D-15. Without this plan, `image_task` cannot prefer Photoroom (either adapter) or fal over incumbents (D-08), and trace would not show why a provider was chosen (D-26).

Output: Three new eval case files under `eval/cases/` (the Photoroom file contains BOTH op kinds), executor/planner trace enrichment for measured/unmeasured + no-incumbent + quality-unavailable surfacing, README docs explaining how to run Phase 11 evals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md
@.planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md
@.planning/phases/11-provider-breadth-post-eval/11-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from src/eval/types.ts, src/eval/cases.ts, src/eval/apply-results.ts, src/runs/trace.ts. -->

From src/eval/types.ts (lines 19-27): EvalCase shape is `{ id, op, provider, fixtureId, params, scorers, requiredEnv? }`. Validation lives in `src/eval/cases.ts isEvalCase` lines 28-46. Plan 01 added `'generate'` to the `CAPABILITY_OPS` set (Task 0). `'composite_layers'` was already in the set since Phase 8.

From src/eval/cases.ts (lines 87-91): cases with `'ocr_text_presence'` in `scorers` MUST include `params.expectedText` (non-empty string).

From src/eval/run.ts (lines 31-37): `getInputPath()` requires `params.input` to be a non-empty string. For `generate`-op cases (Ideogram), there is no input image. For `composite_layers`-op cases, the existing sharp adapter uses `params.input` as the canvas-reference fixture path (see `eval/cases/composite-layers.json composite-golden-overlay` case as analog). Photoroom composite cases follow the SAME convention. Plan 02 must EITHER (a) make `getInputPath` skip the requirement when the case's op does not need an input image, OR (b) have generate cases supply a placeholder fixture's path. RECOMMENDED: change `getInputPath` to return `undefined` when `evalCase.op === 'generate'` and update its single caller (line 104) to handle the undefined case (the `runScorers` `inputPath` parameter is only used by `scorePixelDelta`).

From src/eval/apply-results.ts: `scoresFromEvalResult` aggregates per-scorer averages and `applyEvalResultsToRegistry` re-registers each capability with `quality.scores` populated. This mechanism already supports `'generate'` once the op is in the union (Plan 01 Task 0). It ALSO already works for `composite_layers:photoroom` because apply-results.ts is keyed on `(op, provider, modelVersion)` and treats new keys identically — verify by inspection that no hardcoded provider list exists.

From src/runs/trace.ts: `TraceNode.metadata?: Record<string, unknown>` is the safe place to add `qualityMeasured: boolean`, `qualityScores?: Record<string, number>`, `noIncumbentComparison?: boolean`, and `qualityUnavailable?: boolean` without changing trace shape. `dag-executor.ts` line 106-113 `sanitizedMetadata` strips only Buffers/typed arrays — primitive booleans and number records pass through.

From src/task/serialize-response.ts: trace passes through serialization; the response-guard rejects Buffers and base64 strings, NOT plain object metadata. New metadata fields are JSON-safe.

From eval/fixtures/manifest.json: existing fixtures usable for Photoroom composite_layers eval are `composite-bg` (512x512 white canvas) and `composite-overlay` (128x128 red square layer). The existing sharp `composite-golden-overlay` case at `eval/cases/composite-layers.json` uses these — Photoroom composite cases SHOULD reuse the same fixtures so head-to-head comparison vs. sharp is valid (D-16 mirrors this principle for fal vs OpenAI; same logic applies here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 11 eval cases (Photoroom extract_subject + composite_layers, fal Flux Kontext, Ideogram) and adapt eval runner for `generate` op</name>
  <files>eval/cases/photoroom.json, eval/cases/fal-flux-kontext.json, eval/cases/ideogram.json, src/eval/run.ts, tests/eval/cases.test.ts, tests/eval/run.test.ts</files>
  <read_first>
    - eval/cases/extract-subject.json (analog for Photoroom extract_subject case shape)
    - eval/cases/composite-layers.json (analog for Photoroom composite_layers case shape — see `composite-golden-overlay` case using `composite-bg` + `composite-overlay` fixtures; Photoroom case mirrors this fixture choice for head-to-head vs sharp)
    - eval/cases/edit-prompt.json (analog for fal Flux Kontext case shape; reuse the SAME fixtures `product-simple`, `text-label`, `text-poster` so head-to-head comparison is valid per D-16)
    - eval/fixtures/manifest.json (existing fixture catalog — do NOT add new fixtures; reuse `composite-bg`/`composite-overlay` for Photoroom composite case, `product-simple`/`person-hair-edge`/`low-contrast-shape` for Photoroom extract cases)
    - src/eval/cases.ts lines 28-91 (isEvalCase + expectedText invariant; Plan 01 added `'generate'` to CAPABILITY_OPS; `'composite_layers'` already present)
    - src/eval/run.ts lines 31-145 (getInputPath, hasUsableEnv, runEval main loop — generate-op cases need `getInputPath` to NOT throw when input is absent)
    - src/eval/scorers.ts (alpha_coverage, pixel_delta, ocr_text_presence — already wired; no new scorers required per PATTERNS.md line 487-491)
    - tests/eval/cases.test.ts (case validation test pattern — add Phase 11 case validity coverage including the new composite_layers case)
    - tests/eval/run.test.ts (eval runner test pattern — add a test that a `generate` case without `params.input` does NOT throw in `getInputPath`)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-13..D-19 — eval strategy locks; D-15 explicitly assigns shadow/background measurement to Image Editing API)
    - .planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md (eval case sections lines 388-484)
    - .planning/phases/11-provider-breadth-post-eval/11-01-SUMMARY.md (Plan 01's actual Photoroom Image Editing API endpoint, request schema, and modelVersion strings — eval cases must reference the SAME modelVersion the adapter declares so apply-results.ts can match `(op, provider, modelVersion)` triples)
  </read_first>
  <action>
    1. Create `eval/cases/photoroom.json` containing AT LEAST these FOUR cases — three for `extract_subject` (D-15: product preservation, alpha edge, low-contrast) AND one for `composite_layers` (D-15: shadow/background quality via Image Editing API, satisfying PROV-01 (with shadow) qualifier):
       ```json
       [
         {
           "id": "extract-photoroom-product-simple",
           "op": "extract_subject",
           "provider": "photoroom",
           "fixtureId": "product-simple",
           "params": { "input": "${fixture.path}" },
           "scorers": ["alpha_coverage", "pixel_delta"],
           "requiredEnv": ["PHOTOROOM_API_KEY"]
         },
         {
           "id": "extract-photoroom-person-hair-edge",
           "op": "extract_subject",
           "provider": "photoroom",
           "fixtureId": "person-hair-edge",
           "params": { "input": "${fixture.path}" },
           "scorers": ["alpha_coverage"],
           "requiredEnv": ["PHOTOROOM_API_KEY"]
         },
         {
           "id": "extract-photoroom-low-contrast",
           "op": "extract_subject",
           "provider": "photoroom",
           "fixtureId": "low-contrast-shape",
           "params": { "input": "${fixture.path}" },
           "scorers": ["alpha_coverage"],
           "requiredEnv": ["PHOTOROOM_API_KEY"]
         },
         {
           "id": "composite-photoroom-product-with-shadow",
           "op": "composite_layers",
           "provider": "photoroom",
           "fixtureId": "composite-bg",
           "params": {
             "input": "${fixture.path}",
             "maxPixelDelta": 0.20,
             "canvas": { "width": 512, "height": 512, "background": { "r": 255, "g": 255, "b": 255, "alpha": 1 } },
             "layers": [
               { "input": "eval/fixtures/composite-overlay.png", "x": 192, "y": 192, "anchor": "top-left" }
             ],
             "shadow": { "enabled": true }
           },
           "scorers": ["pixel_delta"],
           "requiredEnv": ["PHOTOROOM_API_KEY"]
         }
       ]
       ```
       Notes on the composite case:
       - Reuses the SAME fixtures (`composite-bg` + `composite-overlay`) as the existing sharp `composite-golden-overlay` case so head-to-head comparison vs sharp is fixture-fair (matching D-16's principle for fal vs OpenAI).
       - `maxPixelDelta` is intentionally LOOSER (0.20) than the sharp case (0.05) because Photoroom's Image Editing API ADDS shadow and may also tweak relighting — pixel_delta vs the no-shadow reference will diverge by the shadow extent. The case still gates on a sane upper bound (no wild output) without forcing pixel-identity to sharp's deterministic output. The exact threshold may need calibration after first eval run; document any adjustment in the SUMMARY.
       - `params.shadow.enabled: true` engages Photoroom's shadow output (Plan 01 Task 1 added this param). The Photoroom adapter surfaces `metadata.shadowApplied: true` so the eval result manifest captures that the (with shadow) path was actually exercised.
       - `scorers: ["pixel_delta"]` measures bounded divergence from the canvas reference. A future pass (Phase 12+) may add a dedicated shadow-presence scorer; for Phase 11 the pixel_delta upper bound + the adapter-side `shadowApplied` metadata flag are sufficient to satisfy PROV-05's "at least one eval case before quality.score is populated" requirement for `composite_layers:photoroom`.
    2. Create `eval/cases/fal-flux-kontext.json` containing AT LEAST these two cases (D-16 head-to-head + D-17 instruction success via OCR):
       ```json
       [
         {
           "id": "edit-fal-kontext-product-color",
           "op": "edit_prompt",
           "provider": "fal",
           "fixtureId": "product-simple",
           "params": {
             "input": "${fixture.path}",
             "minPixelDelta": 0.01,
             "prompt": "Change the product rectangle to a saturated blue while preserving the off-white background."
           },
           "scorers": ["pixel_delta"],
           "requiredEnv": ["FAL_KEY"]
         },
         {
           "id": "edit-fal-kontext-text-label",
           "op": "edit_prompt",
           "provider": "fal",
           "fixtureId": "text-label",
           "params": {
             "input": "${fixture.path}",
             "minPixelDelta": 0.01,
             "prompt": "Change the label text from SALE 25 to SALE 50 while keeping the label style.",
             "expectedText": "SALE 50"
           },
           "scorers": ["pixel_delta", "ocr_text_presence"],
           "requiredEnv": ["FAL_KEY"]
         }
       ]
       ```
       Note: prompts/fixtures MUST exactly match the existing OpenAI cases in `eval/cases/edit-prompt.json` so head-to-head comparison is valid per D-16. The fal `requestId` and OpenAI `revised_prompt` will both be in the per-result manifest; comparison happens at `quality.scores` aggregation time in `apply-results.ts`.
    3. Create `eval/cases/ideogram.json` containing AT LEAST these two cases (D-18 text-heavy generation):
       ```json
       [
         {
           "id": "generate-ideogram-text-fresh-roast",
           "op": "generate",
           "provider": "ideogram",
           "fixtureId": "text-label",
           "params": {
             "prompt": "A product label reading FRESH ROAST in large bold letters on a cream background.",
             "expectedText": "FRESH ROAST"
           },
           "scorers": ["ocr_text_presence"],
           "requiredEnv": ["IDEOGRAM_API_KEY"]
         },
         {
           "id": "generate-ideogram-text-poster-open",
           "op": "generate",
           "provider": "ideogram",
           "fixtureId": "text-poster",
           "params": {
             "prompt": "A vintage poster reading OPEN in large block letters across the top half.",
             "expectedText": "OPEN"
           },
           "scorers": ["ocr_text_presence"],
           "requiredEnv": ["IDEOGRAM_API_KEY"]
         }
       ]
       ```
       Note: `fixtureId` is referenced for the run pipeline's path resolution but NOT used by `ocr_text_presence` (it scores OUTPUT only; see `scorers.ts` line 69-105 — `inputPath` parameter is unused for OCR).
    4. Edit `src/eval/run.ts`:
       - Modify `getInputPath(evalCase: EvalCase): string | undefined` to return `undefined` (instead of throwing) when `evalCase.op === 'generate'` AND `params.input` is absent. Keep the throw for ops where input is required and missing (including `composite_layers` — Photoroom composite cases provide `params.input` per the case shape above, so the existing throw remains the right behavior for them).
       - Update the single caller at line 104: change `const inputPath = getInputPath(evalCase);` so that downstream calls handle the undefined case. The only consumer of `inputPath` is `runScorers(inputPath, outputPath, ...)` line 119-125 — pass an empty string or change the type. Inspect `runScorers` (already loaded): `inputPath: string` is only consumed by `scorePixelDelta` line 222-227. For generate cases, `pixel_delta` is NOT in their `scorers` array, so passing `''` is safe. RECOMMENDED: change `runScorers` signature `inputPath` to `string | undefined`, update `scorePixelDelta` callsite to error if `inputPath` is missing AND scorer is `pixel_delta`. This is a tighter invariant.
    5. Edit `tests/eval/cases.test.ts` to add validation that the three new case files load without error and each case satisfies `isEvalCase` — explicitly assert `eval/cases/photoroom.json` contains BOTH `op: 'extract_subject'` AND `op: 'composite_layers'` cases for `provider: 'photoroom'`. Reuse the existing `loadEvalCases` test pattern.
    6. Edit `tests/eval/run.test.ts` to add tests:
       - When `IDEOGRAM_API_KEY` is unset, the Ideogram cases are skipped (status 'skipped') with `error: 'missing required env: IDEOGRAM_API_KEY'`. When mocked to be set + a stubbed `generate` capability returns a buffer, the case scores via `ocr_text_presence` against the OUTPUT (mock `recognizeOnce` to return a string containing `'FRESH ROAST'`). Mirror the existing skipped-env tests.
       - When `PHOTOROOM_API_KEY` is unset, BOTH the extract_subject AND composite_layers Photoroom cases are skipped. When mocked to be set + a stubbed `composite_layers:photoroom` capability returns a buffer, the case scores via `pixel_delta` against the input fixture (mock returns a buffer that satisfies `maxPixelDelta: 0.20`).
  </action>
  <verify>
    <automated>npx vitest run tests/eval/cases.test.ts tests/eval/run.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls eval/cases/photoroom.json eval/cases/fal-flux-kontext.json eval/cases/ideogram.json` lists all three files.
    - `cat eval/cases/photoroom.json | jq '[.[] | select(.provider=="photoroom")] | length'` returns at least `4` (was 3; now 3 extract + 1 composite).
    - `cat eval/cases/photoroom.json | jq '[.[] | select(.op=="composite_layers" and .provider=="photoroom")] | length'` returns at least `1` (the (with shadow) coverage for PROV-01).
    - `cat eval/cases/photoroom.json | jq '[.[] | select(.op=="extract_subject" and .provider=="photoroom")] | length'` returns at least `3`.
    - `cat eval/cases/photoroom.json | jq '.[] | select(.op=="composite_layers") | .params.shadow.enabled'` returns `true` (shadow path explicitly engaged).
    - `cat eval/cases/fal-flux-kontext.json | jq '[.[] | select(.provider=="fal")] | length'` returns at least `2`.
    - `cat eval/cases/ideogram.json | jq '[.[] | select(.provider=="ideogram" and .op=="generate")] | length'` returns at least `2`.
    - `cat eval/cases/ideogram.json | jq '[.[] | select(.scorers | index("ocr_text_presence")) | .params.expectedText] | length'` returns at least `2` (every Ideogram case has expectedText, satisfying cases.ts line 87-91).
    - `cat eval/cases/fal-flux-kontext.json | jq '[.[] | .fixtureId] | sort | unique | inside(["product-simple", "text-label", "text-poster"])'` returns `true` (only existing fixtures used).
    - `cat eval/cases/photoroom.json | jq '[.[] | .fixtureId] | sort | unique | inside(["product-simple", "person-hair-edge", "low-contrast-shape", "composite-bg"])'` returns `true` (only existing fixtures used).
    - `grep -E "evalCase.op === 'generate'|op === \"generate\"" src/eval/run.ts | wc -l | tr -d ' '` returns at least `1` (the input-skipping branch).
    - `npx vitest run tests/eval/cases.test.ts tests/eval/run.test.ts` exits 0.
    - With API keys present, `npm run eval` (manual smoke; not in automated gate) writes a result JSON containing scored entries for at least one case from each new provider AND for BOTH Photoroom op kinds, AND `applyEvalResultsToRegistry` populates `capabilityRegistry.get('composite_layers', 'photoroom')?.quality?.scores` with at least one scorer key.
  </acceptance_criteria>
  <done>
    Three eval case files validate against `isEvalCase`. The Photoroom file covers BOTH op kinds (extract_subject + composite_layers (with shadow)) so PROV-01 is fully exercised by evals. The eval runner correctly handles `generate`-op cases that lack `params.input`. Tests assert env-skipping and OCR-scoring on Ideogram outputs PLUS env-skipping and pixel_delta scoring on Photoroom composite outputs. PROV-05 satisfied for all four new providers with the (with shadow) qualifier explicitly engaged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface routing transparency in trace (qualityMeasured, noIncumbentComparison, qualityUnavailable, no-silent-fallback)</name>
  <files>src/task/dag-executor.ts, src/task/planner.ts, src/task/serialize-response.ts, tests/task/dag-executor.test.ts, tests/task/planner.test.ts</files>
  <read_first>
    - src/task/dag-executor.ts lines 145-194 (runNodeWithRetry — where TraceNode is constructed; new metadata fields slot in here)
    - src/task/dag-executor.ts lines 106-113 (sanitizedMetadata — confirm primitives pass through)
    - src/runs/trace.ts (TraceNode shape — `metadata?: Record<string, unknown>` is the field to enrich)
    - src/task/planner.ts lines 62-82 (capability snapshot the planner sees — quality.scores already passed; this task adds the same routing-transparency fields the planner can include in its `routingNotes`)
    - src/task/plan-schema.ts lines 43-47 (routingNotes — already supports `measuredQuality: boolean` and `rationale: string` per node)
    - src/task/serialize-response.ts (response-guard — confirm new metadata primitives are not stripped)
    - src/capabilities/registry.ts (use `registry.list(op)` to count incumbents and `registry.listScored(op)` to detect quality-unavailable)
    - tests/task/dag-executor.test.ts (existing TraceNode metadata assertion pattern)
    - tests/task/planner.test.ts (existing snapshot pattern for capability metadata)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-09, D-11, D-25, D-26, D-27)
  </read_first>
  <behavior>
    - Every TraceNode produced by `dag-executor.ts` for an image-or-data capability invocation has `metadata.qualityMeasured: boolean`. True iff `cap.quality?.scores` is non-empty.
    - When `qualityMeasured === true`, also include `metadata.qualityScores: Record<string, number>` (the actual per-scorer scores from `cap.quality.scores`) so the trace shows WHICH scorer drove routing (D-24 goal-specific metrics visibility).
    - When `cap` is the only registered provider for `cap.op` (i.e. `registry.list(cap.op).length === 1`), include `metadata.noIncumbentComparison: true` (D-11). Do NOT emit this field when there are >1 providers for the op (omit, do not set false, to keep traces compact).
    - When the planner snapshot for the chosen node's op contains zero scored providers (i.e. `registry.listScored(cap.op).length === 0`), include `metadata.qualityUnavailable: true` (D-25). Otherwise omit.
    - Provider failures (CapabilityInvokeError) continue to populate `errorDetail` (already implemented in dag-executor.ts lines 115-122). Add a regression test asserting that on PROVIDER_FAILURE, the trace's failed node has `outcome: 'error'` AND no other node retroactively becomes the "chosen" provider — i.e. there is no silent fallback (D-27).
    - The planner system prompt in `planner.ts buildSystemPrompt` is updated to instruct: "When you choose a provider that is the only registered provider for its op, set `routingNotes[i].rationale` to mention 'no incumbent comparison'. When no providers for an op have measured quality, set `routingNotes[i].measuredQuality` to false and explain in rationale that routing was on cost/latency only." This is advisory; the dag-executor's `metadata` is the source of truth for trace consumers.
  </behavior>
  <action>
    1. Edit `src/task/dag-executor.ts runNodeWithRetry` (lines 145-194):
       - Before line 165 (where the success TraceNode is built), compute three values from the resolved registry (use the `registry` already in scope via `ctx.registry ?? capabilityRegistry` — but `runNodeWithRetry` does not currently receive registry; either pass it in OR compute the same flags one level up in `processNode` lines 255-325 where `cap` and `registry` are both in scope). RECOMMENDED: compute in `processNode` and pass to `runNodeWithRetry` as a fourth param `routingTransparency: { qualityMeasured: boolean; qualityScores?: Record<string, number>; noIncumbentComparison?: true; qualityUnavailable?: true }`.
       - Compute:
         ```typescript
         const sameOpProviders = registry.list(cap.op as CapabilityOp);
         const scoredCount = registry.listScored(cap.op as CapabilityOp).length;
         const qualityMeasured = Boolean(cap.quality?.scores && Object.keys(cap.quality.scores).length > 0);
         const routingTransparency: Record<string, unknown> = { qualityMeasured };
         if (qualityMeasured) {
           routingTransparency.qualityScores = cap.quality!.scores;
         }
         if (sameOpProviders.length === 1) {
           routingTransparency.noIncumbentComparison = true;
         }
         if (scoredCount === 0) {
           routingTransparency.qualityUnavailable = true;
         }
         ```
       - Merge `routingTransparency` into `sanitizedMetadata(result.metadata)` for the success branch — order matters: `routingTransparency` keys should NOT be overwritten by capability-supplied metadata (preserve audit semantics). Use `{ ...sanitizedMetadata(result.metadata), ...routingTransparency }`.
       - For the error branch (`makeErrorTraceNode`), still emit `qualityMeasured` and `noIncumbentComparison` so failed-route traces are equally informative (use the same routingTransparency object).
    2. Edit `src/task/planner.ts buildSystemPrompt` lines 73-82:
       - Append two sentences to the routing policy line: `"When you choose a provider that is the only registered provider for its op, set the corresponding routingNotes[i].rationale to include 'no incumbent comparison'. When no providers for an op have measured quality, set routingNotes[i].measuredQuality to false and explain that routing used cost/latency only."`
    3. Inspect `src/task/serialize-response.ts` and confirm new metadata primitives pass through. If the response-guard ResponseGuardError throws on any new field (it shouldn't — new fields are booleans + Record<string, number>), adjust the guard's allowlist to include the new field names. Run `tests/task/serialize-response.test.ts` to verify; if it fails, add the new field names to the response guard's allowed-keys logic and add a regression test.
    4. Edit `tests/task/dag-executor.test.ts` add tests:
       - Test A: Single-provider op (e.g. stub a registry with only one capability for op X). Execute a one-node plan. Assert `trace.nodes[0].metadata.noIncumbentComparison === true`.
       - Test B: Two-provider op, neither has scores. Execute. Assert `trace.nodes[0].metadata.qualityMeasured === false` AND `trace.nodes[0].metadata.qualityUnavailable === true`.
       - Test C: Two-provider op, chosen one has scores. Execute. Assert `trace.nodes[0].metadata.qualityMeasured === true` AND `trace.nodes[0].metadata.qualityScores` is a non-empty object AND `trace.nodes[0].metadata.qualityUnavailable` is undefined.
       - Test D (D-27 no-silent-fallback): Two-provider op, chosen one's `invoke` rejects with `CapabilityInvokeError('PROVIDER_FAILURE', ..., true)`. Execute. Assert the trace has `outcome: 'error'` for the chosen provider AND the other registered provider was NOT invoked (assert via vi.fn().not.toHaveBeenCalled() on the other capability's invoke mock).
    5. Edit `tests/task/planner.test.ts` add a test asserting the system prompt string contains the substring `"no incumbent comparison"` AND `"routingNotes[i].measuredQuality"`. Use the exported `buildSystemPrompt` if available, OR snapshot the message body sent to the Anthropic mock.
  </action>
  <verify>
    <automated>npx vitest run tests/task/dag-executor.test.ts tests/task/planner.test.ts tests/task/serialize-response.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "qualityMeasured" src/task/dag-executor.ts` returns at least `2` (computation + merge into metadata).
    - `grep -c "noIncumbentComparison" src/task/dag-executor.ts` returns at least `1`.
    - `grep -c "qualityUnavailable" src/task/dag-executor.ts` returns at least `1`.
    - `grep -c "listScored" src/task/dag-executor.ts` returns at least `1`.
    - `grep -c "no incumbent comparison" src/task/planner.ts` returns at least `1`.
    - `grep -c "measuredQuality" src/task/planner.ts` returns at least `1` (in the system prompt; `routingNotes` is already typed in plan-schema.ts).
    - `npx vitest run tests/task/dag-executor.test.ts tests/task/planner.test.ts tests/task/serialize-response.test.ts` exits 0 with the four new dag-executor tests + the new planner test passing.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    Trace consumers can see for every executed node: was quality measured, what scores exist, is this an uncontested route, was quality unavailable across all providers. The planner system prompt prescribes the same vocabulary so its `routingNotes` align with executor metadata. No silent provider fallback occurs on `CapabilityInvokeError` (D-27 verified by Test D).
  </done>
</task>

<task type="auto">
  <name>Task 3: README + acceptance smoke — document Phase 11 eval workflow and end-to-end routing visibility (covering all four new capability surfaces)</name>
  <files>README.md, tests/eval/apply-results.test.ts</files>
  <read_first>
    - README.md (find the eval section + capability section to extend; Plan 01 added env-var + capability rows for ALL FOUR adapters including both Photoroom surfaces — this task adds the eval workflow + routing-transparency section)
    - src/eval/apply-results.ts (already populates quality.scores; this task adds an integration test asserting it works for the new providers including `composite_layers:photoroom`)
    - tests/eval/apply-results.test.ts (existing pattern for asserting `applyEvalResultsToRegistry` populates `quality.scores` per capability)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-09, D-10, D-11, D-15, D-25, D-26 — what users should see)
  </read_first>
  <behavior>
    - README has a new section "Phase 11 Provider Breadth" (or extension of an existing eval section) documenting:
      1. How to run Phase 11 evals: `PHOTOROOM_API_KEY=... FAL_KEY=... IDEOGRAM_API_KEY=... npm run eval`.
      2. Which scorers are used per provider AND per Photoroom op kind, and why (D-15/D-16/D-17/D-18 in plain language). Specifically call out that Photoroom `composite_layers` is where shadow/background quality is measured (per D-15) and is the home for the (with shadow) qualifier of PROV-01.
      3. The displacement edges (D-10): a second provider needs ≥0.03 quality-score edge OR ≥20% latency/cost edge before `image_task` will prefer it; `image_op` allows direct exploration regardless (D-07).
      4. How to read trace transparency (D-26): `metadata.qualityMeasured`, `metadata.qualityScores`, `metadata.noIncumbentComparison`, `metadata.qualityUnavailable`. Plus the Photoroom-specific `metadata.api: 'remove-background' | 'image-editing'` and `metadata.shadowApplied: boolean` flags so users can see which Photoroom path was taken.
      5. How to verify after-eval state: `list_capabilities` will show populated `quality.scores` for the new providers including BOTH `extract_subject:photoroom` AND `composite_layers:photoroom`.
    - `tests/eval/apply-results.test.ts` adds a test that constructs a synthetic `EvalRunResult` with scored entries for `extract_subject:photoroom`, `composite_layers:photoroom`, `edit_prompt:fal`, and `generate:ideogram`, registers stub capabilities for those, calls `applyEvalResultsToRegistry`, and asserts each capability's `quality.scores` becomes a non-empty record.
  </behavior>
  <action>
    1. Edit `README.md`:
       - Add or extend a "Phase 11: Provider Breadth (Post-Eval)" subsection. Cover the five points listed in `<behavior>`.
       - Include a small JSON example of a trace node showing the new metadata fields (use the Photoroom composite_layers case as the example since it shows the (with shadow) qualifier in action):
         ```json
         {
           "id": "n1", "op": "composite_layers", "provider": "photoroom",
           "outcome": "success",
           "metadata": {
             "provider": "photoroom",
             "modelVersion": "photoroom-image-editing-v1",
             "api": "image-editing",
             "shadowApplied": true,
             "qualityMeasured": true,
             "qualityScores": { "pixel_delta": 0.12 }
           }
         }
         ```
       - Note that `image_op` allows immediate exploration of new providers but `image_task` planner preference requires eval scores (D-07/D-08).
       - Explicitly document: "PROV-01 is satisfied by TWO Photoroom adapters — `extract_subject` (Remove Background API, no shadow) AND `composite_layers` (Image Editing API, with shadow per D-15). The `(with shadow)` qualifier is delivered by the composite_layers adapter; the eval case `composite-photoroom-product-with-shadow` exercises this path with `params.shadow.enabled: true`."
    2. Edit `tests/eval/apply-results.test.ts` adding the multi-provider integration test described in `<behavior>`. Use a fresh `new CapabilityRegistry()`. Register four stub capabilities: `extract_subject:photoroom` (with `allowUnscoredProduction: true` since `@imgly/local`-equivalent stub is also registered first), `composite_layers:photoroom` (with allowUnscoredProduction since `sharp`-equivalent stub registered first), `edit_prompt:fal` (with allowUnscoredProduction since `openai`-equivalent stub registered first), and `generate:ideogram` (no options — sole provider). Construct an `EvalRunResult` with `status: 'scored'` entries matching their `(op, provider, modelVersion)` keys (for Photoroom: use `'photoroom-remove-bg-v2'` for extract and `'photoroom-image-editing-v1'` for composite — these MUST match the modelVersion strings declared in the Plan 01 adapter files) and a single scored value each. Call `applyEvalResultsToRegistry`. Assert each capability now has `quality.scores` with the expected scorer keys and values. Assert specifically that `registry.listScored('composite_layers')` includes `photoroom`.
  </action>
  <verify>
    <automated>npx vitest run tests/eval/apply-results.test.ts &amp;&amp; grep -c "Phase 11" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 11" README.md` returns at least `1`.
    - `grep -c "qualityMeasured\|qualityScores" README.md` returns at least `2`.
    - `grep -c "PHOTOROOM_API_KEY\|FAL_KEY\|IDEOGRAM_API_KEY" README.md` returns at least `3` (Plan 01 already added these in env-var table; Plan 02 references them again in the Phase 11 section — that is fine).
    - `grep -c "0.03\|20%" README.md` returns at least `1` (displacement edge documentation per D-10).
    - `grep -c "with shadow\|Image Editing API\|shadowApplied" README.md` returns at least `2` (PROV-01 (with shadow) qualifier explicitly documented in the Phase 11 section).
    - `grep -c "composite_layers.*photoroom\|photoroom.*composite_layers" README.md` returns at least `1` (the composite_layers half of PROV-01 is named).
    - `npx vitest run tests/eval/apply-results.test.ts` exits 0 with the new multi-provider integration test passing.
  </acceptance_criteria>
  <done>
    README explains the Phase 11 eval workflow, the routing-transparency fields, the displacement edges, the two-tier policy (image_op vs image_task), AND the dual-Photoroom-surface design (extract_subject vs composite_layers (with shadow)). The apply-results test proves end-to-end that running evals with the new providers (including BOTH Photoroom adapters) populates `quality.scores` and the registry's gate logic accepts the populated scores.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
# Build + full Phase 11-touching test surface
npm run build
npx vitest run tests/eval/cases.test.ts tests/eval/run.test.ts tests/eval/apply-results.test.ts \
                tests/task/dag-executor.test.ts tests/task/planner.test.ts tests/task/serialize-response.test.ts \
                tests/capabilities/registry-quality.test.ts tests/task/templates.test.ts

# PROV-05 coverage check (now covering BOTH Photoroom op kinds)
ls eval/cases/photoroom.json eval/cases/fal-flux-kontext.json eval/cases/ideogram.json
cat eval/cases/photoroom.json eval/cases/fal-flux-kontext.json eval/cases/ideogram.json | jq -r '.[] | "\(.op):\(.provider) -> \(.scorers | join(\",\"))"'

# PROV-01 (with shadow) coverage explicit check
cat eval/cases/photoroom.json | jq '.[] | select(.op=="composite_layers")'

# Trace transparency surface check
grep -E "qualityMeasured|noIncumbentComparison|qualityUnavailable|listScored" src/task/dag-executor.ts | wc -l
```

End-to-end manual smoke (NOT in automated gate, but documented in SUMMARY): with all three API keys set, `npm run eval` exits 0, writes a result JSON containing scored entries for at least one case per new provider INCLUDING the `composite_layers:photoroom` case, and `applyEvalResultsToRegistry` (called inside `runEval` line 182) populates `quality.scores` for `extract_subject:photoroom`, `composite_layers:photoroom`, `edit_prompt:fal`, and `generate:ideogram`.
</verification>

<success_criteria>
1. PROV-05 satisfied: each of Photoroom (BOTH op kinds), fal, and Ideogram has ≥1 eval case under `eval/cases/` with `requiredEnv` listing its API key.
2. PROV-01 fully satisfied at the eval layer: `eval/cases/photoroom.json` contains BOTH `extract_subject` AND `composite_layers (with shadow)` cases for `provider: 'photoroom'`. The composite case engages `params.shadow.enabled: true` so the Image Editing API path (D-15) is exercised.
3. The eval runner correctly handles `generate`-op cases that have no `params.input`.
4. `applyEvalResultsToRegistry` populates `quality.scores` for `extract_subject:photoroom`, `composite_layers:photoroom`, `edit_prompt:fal`, and `generate:ideogram` after a successful eval run (asserted by integration test in Task 3).
5. Every executed TraceNode includes `metadata.qualityMeasured: boolean`; sole-provider nodes include `metadata.noIncumbentComparison: true`; quality-unmeasured ops include `metadata.qualityUnavailable: true`.
6. The planner system prompt instructs use of `routingNotes[i].measuredQuality` and "no incumbent comparison" rationale.
7. D-27 verified: provider failure does not trigger silent fallback (Task 2 Test D).
8. README documents the Phase 11 eval workflow, routing-transparency fields, displacement edges (D-10), AND the dual-Photoroom-surface design with the (with shadow) qualifier resolution.
9. `npm run build` exits 0; targeted test suites pass.
</success_criteria>

<output>
After completion, create `.planning/phases/11-provider-breadth-post-eval/11-02-SUMMARY.md` recording:
- Eval cases created with their fixtures and scorers, called out separately for Photoroom extract_subject vs Photoroom composite_layers
- The actual `maxPixelDelta` threshold used for the Photoroom composite_layers case (and any post-first-run calibration if the initial 0.20 was off)
- Any modifications to `src/eval/run.ts` (especially the `getInputPath` skip for `generate` op)
- Trace metadata fields added (qualityMeasured/qualityScores/noIncumbentComparison/qualityUnavailable) and where they are computed
- Planner system-prompt edits
- Result of running `npm run eval` with API keys present (or a note that this requires runtime credentials), including confirmation that `quality.scores` populated for `composite_layers:photoroom`
- Confirmation that `image_task` traces now show why a provider was chosen (per D-26), with a worked example that includes the Photoroom shadowApplied/api metadata fields
- Suggested follow-ups (e.g. Phase 12 candidates: dedicated shadow-presence scorer for Photoroom composite, BFL direct adapter, fal-platform abstraction, human/preference eval workflow per Deferred Ideas)
</output>
