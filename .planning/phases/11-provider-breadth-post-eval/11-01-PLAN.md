---
phase: 11-provider-breadth-post-eval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/capabilities/photoroom-extract-subject.ts
  - src/capabilities/fal-edit-prompt.ts
  - src/capabilities/ideogram-generate.ts
  - src/capabilities/types.ts
  - src/capabilities/validation.ts
  - src/capabilities/register.ts
  - src/task/plan-schema.ts
  - src/task/plan-validator.ts
  - src/eval/cases.ts
  - src/index.ts
  - tests/capabilities/photoroom-extract-subject.test.ts
  - tests/capabilities/fal-edit-prompt.test.ts
  - tests/capabilities/ideogram-generate.test.ts
  - tests/capabilities/registry-quality.test.ts
  - tests/task/plan-validator.test.ts
  - tests/task/templates.test.ts
  - README.md
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]
user_setup:
  - service: photoroom
    why: "Phase 11 second-provider for extract_subject (D-04, PROV-01); used for product/subject preservation evals."
    env_vars:
      - name: PHOTOROOM_API_KEY
        source: "Photoroom Console -> https://app.photoroom.com/api-dashboard -> API Keys"
    dashboard_config: []
  - service: fal
    why: "Phase 11 second-provider mirror for edit_prompt via Flux Kontext (D-05, D-06, PROV-02, PROV-03)."
    env_vars:
      - name: FAL_KEY
        source: "fal.ai Dashboard -> https://fal.ai/dashboard/keys"
    dashboard_config: []
  - service: ideogram
    why: "Phase 11 introduces capability-layer 'generate' op (D-02, D-03, PROV-04) for text-fidelity evals."
    env_vars:
      - name: IDEOGRAM_API_KEY
        source: "Ideogram Developer Console -> https://developer.ideogram.ai/ -> API Keys"
    dashboard_config: []

must_haves:
  truths:
    - "Per D-01/D-04/PROV-01: Photoroom registers as a capability-only second provider for `extract_subject`, gracefully skipped when PHOTOROOM_API_KEY is absent."
    - "Per D-05/D-06/PROV-02/PROV-03: fal.ai registers as a capability-only second provider for `edit_prompt` (Flux Kontext model), gracefully skipped when FAL_KEY is absent."
    - "Per D-02/D-03/PROV-04: A new `generate` op is added to `CapabilityOp`, and Ideogram registers as the sole provider for `generate`, gracefully skipped when IDEOGRAM_API_KEY is absent."
    - "Per D-12: Photoroom and fal register through `allowUnscoredProduction: true` with a non-empty `unscoredJustification` referencing pending Phase 11 eval cases; the registry's unscored-second-provider gate is NOT loosened."
    - "Per D-26: each new capability's `invoke` return surfaces `metadata.provider` and `metadata.modelVersion` so downstream Plan 02 trace enrichment has stable fields."
    - "Per D-28: `image_op` zod enum, `image_task` template validation, and PlanSchema.op enum all accept `generate` and the new providers without bypassing existing validation paths."
  artifacts:
    - path: "src/capabilities/photoroom-extract-subject.ts"
      provides: "createPhotoroomExtractSubjectCapability factory + Photoroom Remove Background API invocation"
      exports: ["createPhotoroomExtractSubjectCapability"]
    - path: "src/capabilities/fal-edit-prompt.ts"
      provides: "createFalEditPromptCapability factory + fal.ai Flux Kontext queue invocation"
      exports: ["createFalEditPromptCapability"]
    - path: "src/capabilities/ideogram-generate.ts"
      provides: "createIdeogramGenerateCapability factory + Ideogram 3.0 generate invocation"
      exports: ["createIdeogramGenerateCapability"]
    - path: "src/capabilities/types.ts"
      provides: "Updated `CapabilityOp` union containing 'generate'"
      contains: "| 'generate'"
    - path: "src/task/plan-schema.ts"
      provides: "PlanNodeSchema op enum containing 'generate'"
      contains: "'generate'"
    - path: "src/task/plan-validator.ts"
      provides: "EXPECTED_OUTPUT_KIND map containing generate: 'image'"
      contains: "generate: 'image'"
  key_links:
    - from: "src/capabilities/register.ts"
      to: "createPhotoroomExtractSubjectCapability / createFalEditPromptCapability / createIdeogramGenerateCapability"
      via: "factory invocation + capabilityRegistry.register call"
      pattern: "createPhotoroomExtractSubjectCapability\\(\\)|createFalEditPromptCapability\\(\\)|createIdeogramGenerateCapability\\(\\)"
    - from: "src/capabilities/validation.ts"
      to: "generate op param check"
      via: "if (capability.op === 'generate') { ... }"
      pattern: "capability\\.op === 'generate'"
    - from: "src/index.ts"
      to: "image_op zod enum"
      via: "z.enum([..., 'generate'])"
      pattern: "z\\.enum\\(\\[.*'generate'.*\\]\\)"
---

<objective>
Land four new capability adapters (Photoroom `extract_subject`, fal.ai `edit_prompt` via Flux Kontext, Ideogram `generate`) and the type/registration/validation surface they require so they appear in the registry, in `image_op`, and in `image_task` plans without bypassing Phase 7/9/10 validation gates.

Purpose: Honor PROV-01/02/03/04, D-01..D-06, D-12, D-28. This plan is the structural prerequisite for Plan 02's eval cases and trace enrichment. Capabilities register API-key-gated (graceful null on missing key) and use `allowUnscoredProduction: true` ONLY for second-providers (Photoroom, fal); Ideogram is the sole `generate` provider so the unscored gate does not fire (registry.ts line 24 guard).

Output: Four registry entries (when API keys present), an expanded `CapabilityOp` union, an expanded `image_op` zod enum, and an expanded PlanSchema op enum. No eval cases or score application yet — that is Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md
@.planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from src/capabilities/types.ts and src/capabilities/registry.ts. -->
<!-- Use these directly. Do not re-derive by exploration. -->

From src/capabilities/types.ts (lines 91-123):
```typescript
export type CapabilityInvokeErrorCode =
  | 'INPUT_TOO_LARGE'
  | 'CONSTRAINT_VIOLATION'
  | 'PROVIDER_FAILURE'
  | 'TIMEOUT'
  | 'UNSUPPORTED';

export class CapabilityInvokeError extends Error {
  constructor(
    public readonly code: CapabilityInvokeErrorCode,
    message: string,
    public readonly retryable: boolean,
    public readonly suggestion?: string,
  ) { super(message); this.name = 'CapabilityInvokeError'; }
}

export interface Capability {
  op: CapabilityOp;
  provider: string;
  modelVersion: string;
  constraints: CapabilityConstraints;
  cost: CapabilityCost;
  latencyMsP50?: number;
  quality?: CapabilityQuality;
  invoke: CapabilityInvoke;
}
```

From src/capabilities/registry.ts (lines 11-48): `register(capability, options)` THROWS when `sameOpProviders.length > 0 && !hasScores && !options.allowUnscoredProduction`. When `allowUnscoredProduction: true` is passed, `quality.unscoredJustification` MUST be a non-empty string or registry throws (line 43-46).

From src/capabilities/edit-prompt.ts (lines 41-47): use `resolveOptionalEnv(value)` to filter unresolved `${...}` placeholders BEFORE applying defaults — copy this helper verbatim into each new capability file.

From src/runs/trace.ts: `TraceNode.metadata` is sanitized in dag-executor.ts line 106-113 (Buffers/typed arrays stripped). Provider files MUST place `provider`, `modelVersion`, and `qualityMeasured` in the `metadata` field of the `kind: 'image'` return so Plan 02 can surface routing transparency without altering capability shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Photoroom and fal.ai capability adapters (extract_subject + edit_prompt)</name>
  <files>src/capabilities/photoroom-extract-subject.ts, src/capabilities/fal-edit-prompt.ts, tests/capabilities/photoroom-extract-subject.test.ts, tests/capabilities/fal-edit-prompt.test.ts</files>
  <read_first>
    - src/capabilities/edit-prompt.ts (factory shape, env helper, AbortController timeout, response error mapping — analog for Photoroom)
    - src/capabilities/enhance-upscale.ts (Replicate SDK queue/wait + `fetchWithTimeoutRetry` for URL output — analog for fal.ai)
    - src/capabilities/types.ts (Capability interface, CapabilityInvokeError codes, CapabilityInvokeResult union)
    - src/capabilities/registry.ts lines 24-48 (unscored-second-provider gate + unscoredJustification requirement)
    - src/utils/path-input-root.ts (assertWithinInputRoot — required for params.input validation)
    - tests/capabilities/edit-prompt.test.ts (test pattern: vi.stubGlobal('fetch', fetchMock), env-key beforeEach/afterEach)
    - tests/capabilities/enhance-upscale.test.ts (Replicate SDK mock pattern; mirror for fal client mock)
    - .planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md (Photoroom/fal-specific notes lines 129-220)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-04, D-05, D-06, D-12, D-26)
  </read_first>
  <behavior>
    - Photoroom factory: `createPhotoroomExtractSubjectCapability()` returns `null` when `PHOTOROOM_API_KEY` is unset OR is a literal `${...}` placeholder.
    - When key present: returns Capability with `op: 'extract_subject'`, `provider: 'photoroom'`, `modelVersion: 'photoroom-remove-bg-v2'`, `constraints: { requiresInputImage: true, supportsMultipleInputs: false, outputFormat: 'png' }`, `cost: { perCallUsd: 0.02 }`, `latencyMsP50: 3000`, `quality: { unscoredJustification: 'Phase 11 initial registration; eval scores pending from photoroom eval cases (D-12, PROV-05)' }`.
    - Photoroom invoke: POST to `https://image-api.photoroom.com/v2/segment` with `Authorization: Bearer ${apiKey}` (or `x-api-key: ${apiKey}` per Photoroom docs — verify), multipart form-data `image_file=@<path>`. Use `AbortController` with 30s timeout. Map non-2xx → `CapabilityInvokeError('PROVIDER_FAILURE', ..., true)`; AbortError → `CapabilityInvokeError('TIMEOUT', 'Photoroom segment timed out after 30s', true)`. Return `{ kind: 'image', buffer, model: 'photoroom-remove-bg-v2', metadata: { input: filePath, provider: 'photoroom', modelVersion: 'photoroom-remove-bg-v2', qualityMeasured: false } }`.
    - fal factory: `createFalEditPromptCapability()` returns `null` when `FAL_KEY` is unset/placeholder. When present: `op: 'edit_prompt'`, `provider: 'fal'`, `modelVersion: 'fal-ai/flux-pro/kontext'` (constant; do NOT read from env to avoid version drift bypassing registry.ts line 20-22 score invalidation), `constraints: { requiresInputImage: true, supportsMultipleInputs: false, maxPromptLength: 4000, supportedSizes: ['square', 'landscape', 'portrait'], outputFormat: 'png' }`, `cost: { perCallUsd: 0.04 }`, `latencyMsP50: 6000`, `quality: { unscoredJustification: 'Phase 11 initial registration; eval scores pending from fal-flux-kontext eval cases (D-12, PROV-05)' }`.
    - fal invoke: validate `params.input` (string, non-empty, `assertWithinInputRoot`), `params.prompt` (string, non-empty, len <= 4000). POST to `https://queue.fal.run/fal-ai/flux-pro/kontext` with `Authorization: Key ${apiKey}`, body `{ image_url: 'data:<mime>;base64,<b64>', prompt }`. fal returns either an immediate result or a queue object; for queue case poll `status_url` until `COMPLETED` (max 60s, 2s interval). Then GET the result, extract `images[0].url`, fetch via `fetchWithTimeoutRetry` (copy from enhance-upscale.ts) with 30s timeout, 1 retry. Return `{ kind: 'image', buffer, model: 'fal-ai/flux-pro/kontext', revisedPrompt: undefined, metadata: { input: filePath, provider: 'fal', modelVersion: 'fal-ai/flux-pro/kontext', requestId: <fal queue id>, qualityMeasured: false } }`. All error mapping uses `CapabilityInvokeError` with retryable=true for PROVIDER_FAILURE/TIMEOUT, false for CONSTRAINT_VIOLATION.
    - Tests (vitest): each capability has at least: (a) returns null when API key absent (use `delete process.env.<KEY>`), (b) constraint violation throws `CapabilityInvokeError` code='CONSTRAINT_VIOLATION' for missing input/prompt, (c) successful path with `vi.stubGlobal('fetch', ...)` returning a valid response yields `kind: 'image'` with the mocked buffer + correct metadata fields (`provider`, `modelVersion`, `qualityMeasured: false`), (d) HTTP non-OK maps to `PROVIDER_FAILURE` retryable=true, (e) AbortError maps to `TIMEOUT`. fal test additionally exercises queue polling: first fetch returns `{ status: 'IN_QUEUE', status_url, response_url }`, second fetch returns `{ status: 'COMPLETED' }`, GET response_url returns `{ images: [{ url }] }`, image URL fetch returns the buffer.
  </behavior>
  <action>
    1. Copy `resolveOptionalEnv` helper verbatim from `src/capabilities/edit-prompt.ts` lines 41-47 into both new files.
    2. Create `src/capabilities/photoroom-extract-subject.ts` exporting `createPhotoroomExtractSubjectCapability(): Capability | null`. Env var: `PHOTOROOM_API_KEY`. Endpoint: `https://image-api.photoroom.com/v2/segment`. Use `FormData` with the input file blob (read with `fs.promises.readFile`, wrap as `new Blob([buffer], { type: mime })`). Surface `metadata.qualityMeasured = false` until Plan 02's eval scores land.
    3. Create `src/capabilities/fal-edit-prompt.ts` exporting `createFalEditPromptCapability(): Capability | null`. Env var: `FAL_KEY`. Endpoint: `https://queue.fal.run/fal-ai/flux-pro/kontext`. Implement queue submit -> poll `status_url` (max 30 polls at 2s interval = 60s) -> fetch `response_url` -> download `images[0].url`. Use raw `fetch` (do NOT add `@fal-ai/client` to package.json — keep dependency surface minimal per project preference of `@imgly/local`-style direct integration). Inline a copy of `fetchWithTimeoutRetry` from `enhance-upscale.ts` lines 26-55 (or extract to `src/capabilities/util-fetch.ts` if both files would duplicate it — use your discretion, but if extracted both files import from it).
    4. Use `assertWithinInputRoot(filePath)` for `params.input` validation in both files (see edit-prompt.ts line 87).
    5. Map all error paths to `CapabilityInvokeError` with the exact codes listed in `<behavior>`. The `unscoredJustification` strings MUST be non-empty (registry.ts line 43-46 will throw otherwise on registration).
    6. Tests live under `tests/capabilities/photoroom-extract-subject.test.ts` and `tests/capabilities/fal-edit-prompt.test.ts`. Mirror beforeEach/afterEach env-management from `tests/capabilities/edit-prompt.test.ts` lines 9-22.
  </action>
  <verify>
    <automated>npx vitest run tests/capabilities/photoroom-extract-subject.test.ts tests/capabilities/fal-edit-prompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function createPhotoroomExtractSubjectCapability" src/capabilities/photoroom-extract-subject.ts` returns `1`.
    - `grep -c "export function createFalEditPromptCapability" src/capabilities/fal-edit-prompt.ts` returns `1`.
    - `grep -c "PHOTOROOM_API_KEY" src/capabilities/photoroom-extract-subject.ts` returns at least `1`.
    - `grep -c "FAL_KEY" src/capabilities/fal-edit-prompt.ts` returns at least `1`.
    - `grep -c "fal-ai/flux-pro/kontext" src/capabilities/fal-edit-prompt.ts` returns at least `1`.
    - `grep -c "unscoredJustification" src/capabilities/photoroom-extract-subject.ts` returns at least `1`.
    - `grep -c "unscoredJustification" src/capabilities/fal-edit-prompt.ts` returns at least `1`.
    - `grep -c "qualityMeasured: false" src/capabilities/photoroom-extract-subject.ts src/capabilities/fal-edit-prompt.ts` returns at least `2` total.
    - `grep -E "CapabilityInvokeError\\('TIMEOUT'" src/capabilities/photoroom-extract-subject.ts src/capabilities/fal-edit-prompt.ts | wc -l | tr -d ' '` returns at least `2`.
    - `npx vitest run tests/capabilities/photoroom-extract-subject.test.ts tests/capabilities/fal-edit-prompt.test.ts` exits 0 with at least 5 tests per file.
  </acceptance_criteria>
  <done>
    Both capability files compile under `npm run build`. Both test files pass. Factories return `null` when API keys are unset. When keys present and registered (next task), the registry validates the `unscoredJustification` and accepts both as second-providers via `allowUnscoredProduction: true`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `generate` op + Ideogram capability + type/schema/validator/enum wiring</name>
  <files>src/capabilities/types.ts, src/capabilities/ideogram-generate.ts, src/capabilities/validation.ts, src/task/plan-schema.ts, src/task/plan-validator.ts, src/eval/cases.ts, src/index.ts, tests/capabilities/ideogram-generate.test.ts, tests/task/plan-validator.test.ts</files>
  <read_first>
    - src/capabilities/types.ts (current CapabilityOp union lines 1-9 — add `'generate'`)
    - src/capabilities/edit-prompt.ts (factory + prompt validation + ephemeral URL pattern — analog for Ideogram)
    - src/capabilities/enhance-upscale.ts lines 26-55 (fetchWithTimeoutRetry — Ideogram returns ephemeral URLs that must be downloaded before TTL expires)
    - src/capabilities/validation.ts (current op-block pattern lines 19-23 for edit_prompt — mirror for generate)
    - src/task/plan-schema.ts (PlanNodeSchema.op enum lines 17-26 — add `'generate'`)
    - src/task/plan-validator.ts (EXPECTED_OUTPUT_KIND map lines 45-54 — add `generate: 'image'`)
    - src/eval/cases.ts (CAPABILITY_OPS Set lines 17-26 — add `'generate'`)
    - src/index.ts lines 452-470 (image_op zod enum line 457 — add `'generate'`)
    - .planning/phases/11-provider-breadth-post-eval/11-PATTERNS.md (Ideogram-specific notes lines 223-310, types.ts modify section lines 290-310)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-02, D-03, D-11, D-18)
    - tests/task/plan-validator.test.ts (existing pattern for adding a new op to EXPECTED_OUTPUT_KIND test)
  </read_first>
  <behavior>
    - `CapabilityOp` union in `src/capabilities/types.ts` includes `'generate'` as a new entry.
    - `createIdeogramGenerateCapability()`: returns `null` when `IDEOGRAM_API_KEY` is unset/placeholder. When present: `op: 'generate'`, `provider: 'ideogram'`, `modelVersion: 'ideogram-v3-0'`, `constraints: { requiresInputImage: false, supportsMultipleInputs: false, maxPromptLength: 4000, supportedSizes: ['square', 'landscape', 'portrait'], outputFormat: 'png' }`, `cost: { perCallUsd: 0.08 }`, `latencyMsP50: 8000`. Note: Ideogram is the SOLE provider for `generate`, so the registry's unscored-second-provider gate does NOT fire — DO NOT pass `allowUnscoredProduction: true` (it would require unscoredJustification but is unnecessary; keep `quality` undefined until Plan 02 eval scores land per D-11).
    - Ideogram invoke: validate `params.prompt` (non-empty string, ≤4000 chars). Optional `params.size` maps to Ideogram aspect ratio (`square` → `ASPECT_1_1`, `landscape` → `ASPECT_16_9`, `portrait` → `ASPECT_9_16`). POST to `https://api.ideogram.ai/v1/ideogram-v3/generate` with header `Api-Key: ${apiKey}` and body `{ prompt, aspect_ratio, rendering_speed: 'DEFAULT', num_images: 1 }`. Response shape: `{ data: [{ url, seed }] }`. Download the URL with `fetchWithTimeoutRetry` (copy from enhance-upscale.ts or import shared util if extracted in Task 1) — Ideogram URLs are ephemeral (~1h TTL). Return `{ kind: 'image', buffer, model: 'ideogram-v3-0', metadata: { prompt, seed: data[0].seed, provider: 'ideogram', modelVersion: 'ideogram-v3-0', qualityMeasured: false } }`.
    - `validateCapabilityParams` adds a `generate` block: requires non-empty `params.prompt`. Existing `maxPromptLength` and `supportedSizes` constraint checks (lines 96-114) cover prompt length and size validation generically.
    - `PlanNodeSchema.op` zod enum includes `'generate'`. `EXPECTED_OUTPUT_KIND` in plan-validator.ts maps `generate: 'image'`. `CAPABILITY_OPS` set in eval/cases.ts includes `'generate'`. `image_op` zod enum in index.ts line 457 includes `'generate'`.
    - Tests: (a) `tests/capabilities/ideogram-generate.test.ts`: returns null on missing key; throws CONSTRAINT_VIOLATION on missing/empty prompt; throws CONSTRAINT_VIOLATION on prompt > 4000 chars; happy path mocks fetch to return `{ data: [{ url: 'https://...', seed: 42 }] }` then a buffer fetch and asserts metadata contains `provider: 'ideogram'`, `modelVersion: 'ideogram-v3-0'`, `qualityMeasured: false`, `seed: 42`. (b) `tests/task/plan-validator.test.ts` adds at minimum: a passing plan with one `generate` node (`outputKind: 'image'`, `terminalNodeId` matches) validates ok; a plan with `generate` op + `outputKind: 'data'` returns `errors[].code === 'PLAN_OUTPUT_KIND_MISMATCH'`.
  </behavior>
  <action>
    1. Edit `src/capabilities/types.ts` line 1-9: add `| 'generate'` as the LAST entry of the `CapabilityOp` union (preserve alphabetical-ish order at the end, do not reorder existing entries to minimize diff).
    2. Create `src/capabilities/ideogram-generate.ts` exporting `createIdeogramGenerateCapability(): Capability | null`. Env var: `IDEOGRAM_API_KEY`. Use the shared/inlined `fetchWithTimeoutRetry` for ephemeral URL download. Aspect ratio map MUST be defined as a const: `const ASPECT_MAP = { square: 'ASPECT_1_1', landscape: 'ASPECT_16_9', portrait: 'ASPECT_9_16' } as const;`. Constant model version: `const MODEL_VERSION = 'ideogram-v3-0';`. Surface `metadata.qualityMeasured = false`.
    3. Edit `src/capabilities/validation.ts`: add a new block immediately after the `edit_prompt` block (after line 23):
       ```typescript
       if (capability.op === 'generate') {
         if (typeof params.prompt !== 'string' || params.prompt.trim().length === 0) {
           throw new Error('generate requires params.prompt');
         }
       }
       ```
    4. Edit `src/task/plan-schema.ts` line 17-26: add `'generate',` to the `op` zod enum (any position, but place last to minimize diff).
    5. Edit `src/task/plan-validator.ts` lines 45-54: add `generate: 'image',` to `EXPECTED_OUTPUT_KIND`.
    6. Edit `src/eval/cases.ts` lines 17-26: add `'generate',` to `CAPABILITY_OPS` Set.
    7. Edit `src/index.ts` line 457: change the `image_op` `op` zod enum from `z.enum(['extract_subject', 'edit_prompt', 'composite_layers', 'transform', 'enhance_upscale', 'analyze_dimensions', 'analyze_palette', 'analyze_ocr'])` to include `'generate'` as the last entry. Update the surrounding tool description string at line 454 to mention `generate` in the image-returning ops list.
    8. Tests: write `tests/capabilities/ideogram-generate.test.ts` mirroring `tests/capabilities/edit-prompt.test.ts` env management. Extend `tests/task/plan-validator.test.ts` with the two `generate` cases described in `<behavior>` (you may need to extend a Capability fixture — search the existing test for capability mock pattern and add `{ op: 'generate', provider: 'ideogram', modelVersion: 'ideogram-v3-0', constraints: {...}, cost: {...}, invoke: vi.fn() }`).
  </action>
  <verify>
    <automated>npx vitest run tests/capabilities/ideogram-generate.test.ts tests/task/plan-validator.test.ts tests/task/plan-schema.test.ts tests/eval/cases.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "^\s*\|\s*'generate'" src/capabilities/types.ts | wc -l | tr -d ' '` returns at least `1`.
    - `grep -c "'generate'" src/task/plan-schema.ts` returns at least `1`.
    - `grep -c "generate: 'image'" src/task/plan-validator.ts` returns at least `1`.
    - `grep -c "'generate'" src/eval/cases.ts` returns at least `1`.
    - `grep -c "'generate'" src/index.ts` returns at least `1` (within the image_op zod enum line).
    - `grep -c "capability.op === 'generate'" src/capabilities/validation.ts` returns at least `1`.
    - `grep -c "createIdeogramGenerateCapability" src/capabilities/ideogram-generate.ts` returns at least `1`.
    - `grep -c "IDEOGRAM_API_KEY" src/capabilities/ideogram-generate.ts` returns at least `1`.
    - `grep -c "ideogram-v3-0" src/capabilities/ideogram-generate.ts` returns at least `2` (modelVersion declaration + return metadata).
    - `npm run build` exits 0 (TypeScript compilation must pass with the new union member; this catches any switch/exhaustive checks elsewhere that need updating).
    - `npx vitest run tests/capabilities/ideogram-generate.test.ts tests/task/plan-validator.test.ts tests/task/plan-schema.test.ts tests/eval/cases.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    `generate` is a first-class CapabilityOp throughout the system. Ideogram capability factory returns `null` without `IDEOGRAM_API_KEY` and a fully-shaped `Capability` when present. Plan validation accepts `generate` nodes with `outputKind: 'image'` and rejects them with `outputKind: 'data'`. `image_op` accepts `op: 'generate'`. TypeScript compilation passes with no exhaustiveness errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire factories in register.ts + extend templates registry validation tests + README docs</name>
  <files>src/capabilities/register.ts, tests/capabilities/registry-quality.test.ts, tests/task/templates.test.ts, README.md</files>
  <read_first>
    - src/capabilities/register.ts (current registration order + null-guard pattern lines 11-31)
    - src/capabilities/registry.ts lines 24-48 (unscored gate + unscoredJustification audit; this is what `allowUnscoredProduction: true` toggles)
    - src/task/templates.ts lines 180-199 (matchTemplate registry validation — adding new providers must NOT bypass this; templates check `registry.get(node.op, node.provider)` for every node)
    - tests/capabilities/registry-quality.test.ts (existing test for unscored second-provider gate; add coverage for Photoroom + fal new-second-provider scenarios)
    - tests/task/templates.test.ts (existing template-match assertions; add a sanity test asserting `matchTemplate` still works after registering Photoroom/fal/Ideogram)
    - README.md (find the env vars section + capability/provider tables to extend)
    - .planning/phases/11-provider-breadth-post-eval/11-CONTEXT.md (D-12, D-28)
  </read_first>
  <behavior>
    - `registerBuiltInCapabilities()` invokes the three new factories AFTER existing registrations so registration order is: extract_subject (@imgly/local), edit_prompt (openai), transform, analyze_dimensions, analyze_palette, composite_layers, enhance_upscale, analyze_ocr, photoroom-extract-subject, fal-edit-prompt, ideogram-generate.
    - Photoroom registers via `capabilityRegistry.register(photoroomExtract, { allowUnscoredProduction: true })` because `@imgly/local` is already a same-op provider.
    - fal registers via `capabilityRegistry.register(falEditPrompt, { allowUnscoredProduction: true })` because `openai` is already a same-op provider.
    - Ideogram registers via plain `capabilityRegistry.register(ideogramGenerate)` (no options) because `generate` has no incumbent — registry's `sameOpProviders.length > 0` guard does not fire.
    - When API keys are absent, the corresponding factory returns `null` and `register.ts` MUST guard with `if (factory) { ... }` so absence is non-fatal.
    - `tests/capabilities/registry-quality.test.ts` adds: (a) registering a Photoroom-shaped capability without `allowUnscoredProduction: true` THROWS the unscored-gate error from registry.ts line 36-38; (b) registering with `allowUnscoredProduction: true` BUT empty `unscoredJustification` THROWS line 44-46 error; (c) registering with both `allowUnscoredProduction: true` AND non-empty justification SUCCEEDS.
    - `tests/task/templates.test.ts` adds: with all three new factories' API keys set in `process.env` and capabilities registered, `matchTemplate({ goal: 'product-on-white', inputImages: { input_0: '/abs/path/img.png' }})` still returns a `TemplateMatch` whose plan nodes all resolve via `registry.get` (D-28 — adding new providers must not break template fast-path validation).
    - README documents `PHOTOROOM_API_KEY`, `FAL_KEY`, `IDEOGRAM_API_KEY` in the env-var section and lists the new capabilities (`extract_subject` via `photoroom`, `edit_prompt` via `fal`, `generate` via `ideogram`) in the capability/provider table.
  </behavior>
  <action>
    1. Edit `src/capabilities/register.ts`:
       - Add three imports at top: `import { createPhotoroomExtractSubjectCapability } from './photoroom-extract-subject.js';`, `import { createFalEditPromptCapability } from './fal-edit-prompt.js';`, `import { createIdeogramGenerateCapability } from './ideogram-generate.js';`.
       - After the existing `createAnalyzeOcrCapability()` registration (after line 28), add three null-guarded registration blocks in this exact order:
         ```typescript
         const photoroomExtract = createPhotoroomExtractSubjectCapability();
         if (photoroomExtract) {
           capabilityRegistry.register(photoroomExtract, { allowUnscoredProduction: true });
         }
         const falEditPrompt = createFalEditPromptCapability();
         if (falEditPrompt) {
           capabilityRegistry.register(falEditPrompt, { allowUnscoredProduction: true });
         }
         const ideogramGenerate = createIdeogramGenerateCapability();
         if (ideogramGenerate) {
           capabilityRegistry.register(ideogramGenerate);
         }
         ```
    2. Edit `tests/capabilities/registry-quality.test.ts`:
       - Add a `describe('Phase 11 second-provider gate')` block covering the three sub-cases in `<behavior>` (a/b/c). Use minimal inline Capability literals — do NOT import the real factories (the real factories require API keys); use a fresh `new CapabilityRegistry()` per test. Pre-register a same-op incumbent first (e.g. `{ op: 'extract_subject', provider: 'fake-incumbent', modelVersion: 'v1', ... }`), then assert behaviors for the second registration.
    3. Edit `tests/task/templates.test.ts`:
       - Add an assertion using a stub registry (the file already pattern-matches templates against a registry) that includes Photoroom, fal, and Ideogram entries alongside the incumbents, and verify `matchTemplate({ goal: 'product-on-white', ... })` returns a non-null `TemplateMatch`. Reuse the file's existing stub-registry pattern; do not introduce new test infra.
    4. Edit `README.md`:
       - In the env vars section, append three rows: `PHOTOROOM_API_KEY` ("Photoroom API key. Enables `extract_subject` via `photoroom` for product/subject preservation."), `FAL_KEY` ("fal.ai API key. Enables `edit_prompt` via `fal` (Flux Kontext) as a faster/cheaper mirror to `openai`."), `IDEOGRAM_API_KEY` ("Ideogram Developer API key. Enables `generate` via `ideogram` for text-fidelity generation.").
       - In the capability table (or `image_op` capabilities section), add three rows mapping op → provider → API key requirement → notes.
       - Note explicitly: "Photoroom, fal, and Ideogram are exposed via `image_op` once their API keys are present. `image_task` will only prefer them once Phase 11 eval cases populate quality scores (see Plan 02)."
  </action>
  <verify>
    <automated>npm run build && npx vitest run tests/capabilities/registry-quality.test.ts tests/task/templates.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "createPhotoroomExtractSubjectCapability" src/capabilities/register.ts` returns at least `2` (import + invocation).
    - `grep -c "createFalEditPromptCapability" src/capabilities/register.ts` returns at least `2`.
    - `grep -c "createIdeogramGenerateCapability" src/capabilities/register.ts` returns at least `2`.
    - `grep -c "allowUnscoredProduction: true" src/capabilities/register.ts` returns at least `3` (existing enhance_upscale + new Photoroom + new fal). Ideogram MUST NOT have this option in its `register(...)` call.
    - `grep -E "ideogramGenerate\\)" src/capabilities/register.ts | grep -v "allowUnscoredProduction" | wc -l | tr -d ' '` returns at least `1` (Ideogram registration without options).
    - `grep -c "PHOTOROOM_API_KEY\|FAL_KEY\|IDEOGRAM_API_KEY" README.md` returns at least `3`.
    - `npx vitest run tests/capabilities/registry-quality.test.ts tests/task/templates.test.ts` exits 0.
    - `npm run build` exits 0.
    - With all three API keys set in env and `dist/index.js` started, manual smoke (NOT in automated gate but documented in done): calling `list_capabilities` returns entries for `extract_subject:photoroom`, `edit_prompt:fal`, `generate:ideogram`.
  </acceptance_criteria>
  <done>
    All three new capabilities are wired into `registerBuiltInCapabilities()`. Registration is API-key-gated. The unscored-second-provider gate is preserved (D-12) and exercised by tests. Templates fast-path still validates against the expanded registry (D-28). README documents the new env vars and capability rows. `npm run build` and the targeted vitest suites pass.
  </done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
# Full build + targeted tests
npm run build
npx vitest run tests/capabilities/ tests/task/plan-validator.test.ts tests/task/templates.test.ts tests/task/plan-schema.test.ts tests/eval/cases.test.ts

# Source surface checks
grep -c "'generate'" src/capabilities/types.ts src/task/plan-schema.ts src/eval/cases.ts src/index.ts

# Registration order + gate hygiene
grep -A2 "createPhotoroomExtractSubjectCapability\|createFalEditPromptCapability\|createIdeogramGenerateCapability" src/capabilities/register.ts
```

Each of the four PROV requirements PROV-01, PROV-02, PROV-03, PROV-04 has a corresponding capability file registered in `register.ts` (PROV-05 is satisfied jointly with Plan 02's eval cases). `npm run build` must exit 0.
</verification>

<success_criteria>
1. `src/capabilities/photoroom-extract-subject.ts`, `src/capabilities/fal-edit-prompt.ts`, `src/capabilities/ideogram-generate.ts` exist and export their factory functions.
2. `CapabilityOp` union, `PlanNodeSchema.op` enum, `image_op` zod enum, and `CAPABILITY_OPS` Set all include `'generate'`.
3. `EXPECTED_OUTPUT_KIND` in plan-validator.ts maps `generate: 'image'`.
4. `validateCapabilityParams` enforces non-empty `params.prompt` for `generate`.
5. `register.ts` wires all three new factories; Photoroom and fal use `allowUnscoredProduction: true` with non-empty justifications; Ideogram does not (sole-provider — D-11 trace surfacing handled in Plan 02).
6. All new and modified test files pass.
7. `npm run build` exits 0.
8. README documents the three new env vars and capability rows.
</success_criteria>

<output>
After completion, create `.planning/phases/11-provider-breadth-post-eval/11-01-SUMMARY.md` recording:
- Provider adapters created and which API endpoints they hit
- Any deviations from PATTERNS.md (e.g., Photoroom auth header form, fal queue polling cadence)
- Whether `fetchWithTimeoutRetry` was inlined or extracted to a shared util
- Confirmed list of registered capabilities when the relevant env vars are present
- Notes for Plan 02 on how `metadata.qualityMeasured` should flip to `true` post-eval
</output>
